Reverse Agent 项目进展日志（截至 2026-04-16）

一、已完成内容
1) 核心能力与代码迭代
- 已完成 Auto / Static / Dynamic 三模式联动。
- 已接入并稳定运行工具链自动化：IDA + OllyDbg（含默认脚本）。
- 已新增 angr 后备求解模块（可选依赖，缺失时自动跳过）。
- 已实现大证据场景的提示词自适应精简与 Copilot 超时重试/降级。
- 已实现 GUI 子系统 EXE 的窗口级动态校验（pywinauto）。
- 已完善候选排名、验证矩阵、失败归因与报告脱敏。
- 已增强前缀候选能力（含二进制级 prefix 探测）。

2) 质量保障
- 测试套件当前通过：37 passed（python -m pytest -q）。
- 新增/补充了 pipeline 与模型相关测试用例，覆盖超时、降级、GUI 校验、prefix 逻辑等关键路径。

3) 针对样本 samplereverse.exe 的逆向结论
- 样本路径：E:\xwechat_files\wxid_9ky6h8wz58b912_a1e5\msg\file\2026-04\samplereverse.exe
- 已定位关键链路：
  输入 -> UTF-16LE -> Base64 -> RC4 解密内置密文 -> 比较前 5 个宽字符是否为 “flag{”（大小写不敏感）
- 已确认程序存在固定字符串：
  “flag{”、"Flag : "、"输入的密钥是..."、"密钥不正确"
- 已执行多轮高强度搜索（多字符集、多长度、字典与并行穷举），当前仍未命中最终可通过输入。

二、当前阻塞点
- 问题不在流程稳定性，而在密钥空间规模与样本先验不足：
  现有无额外题面提示（长度/字符集/构造规则）时，纯穷举成本过高。

三、下一次工作内容（执行清单）
1) 先做“可显著降维”的静态增强
- 在 pipeline 中增加“样本特定常量提取器”：
  自动提取 RC4 key 相关约束（长度、字符集判定、前后处理函数指纹）。
- 将提取出的约束写入候选生成器，优先替代盲目暴力。

2) 动态侧补充“断点取真值”
- 通过 Olly/pywinauto 脚本增强：
  在 compare 前抓取实际参与比较的宽字符串缓冲区，直接恢复目标明文前缀与长度信息。
- 将该证据写入 candidates（source=runtime_probe），进入统一排名链路。

3) 搜索策略升级
- 将暴力搜索改为“分层 + 可恢复断点续跑”：
  字符集优先级、长度分段、检查点持久化、失败重放。
- 引入更强字典组合（大小写变体、leet、后缀规则）作为低成本优先层。

4) 验证与交付
- 用 samplereverse.exe 作为固定回归样本执行完整流程。
- 目标：拿到可通过输入并在报告中输出完整证据链。

四、本次新增进展（2026-04-16）
1) 已完成“样本特定常量提取 + 约束化搜索”落地
- 新增 `reverse_agent/sample_solver.py`，在命中 `samplereverse` 特征时自动启用：
  - 识别特征链：`nibble_expand(+0x78/+0x7A) -> UTF-16LE -> Base64 -> RC4 -> 前缀比较 flag{`
  - 自动记录运行证据：`runtime_probe:*`（包含前缀预览、检查点路径等）
  - 支持断点续跑：`solve_reports/tool_artifacts/samplereverse/samplereverse_search_checkpoint.json`
- `pipeline` 已接入该求解器，结果会作为 `SampleProbe` 工具产物参与统一候选排序。

2) 已完成“搜索策略升级（分层 + 断点续跑）”第一版
- 分层输入源：已有候选 + 字符串 token 变体 + 规则化笛卡尔层（默认大写数字集，长度 4~6）。
- 每次运行使用预算 `max_attempts`，超预算自动保存进度，下次从断点继续。

3) 已完成回归测试补充
- 新增 `tests/test_sample_solver.py`。
- 当前测试结果：`python -m pytest -q` -> `40 passed`。

4) 当前求解状态
- 已对目标样本执行多轮续跑搜索（累计推进到长度 5 的搜索空间），尚未命中可通过输入。
- 最新断点位置：`resume_len=5`，`resume_idx=469311`（可继续迭代）。

五、本轮收尾进展（按“半小时未完成则终止并收尾”执行）
1) 已完成的关键验证
- 结合反汇编确认核心判定分支（`0x40258C`）仅检查：
  `__wcsnicmp(decrypted, L"flag{", 5)`。
- 已新增并修复 `sample_solver` 的更深层依赖探测与 checkpoint 字段持久化：
  - L7/m40 依赖探测（prefix4）
  - L8/m44 与 L9/m48 依赖探测（prefix5）
  - 新 checkpoint 字段：`dep_*`、`m44_*`、`m48_*`
- 当前回归测试：`python -m pytest -q` -> `43 passed`。

2) 已完成的高强度穷举结果（低优先级执行，避免压垮主机）
- m40（L7 依赖4字节）全字节穷举：`2^32`，结果 `NO_HIT`。
- m48（L9 依赖5字节，hi4 模型）全空间穷举：`2^36`，结果 `NO_HIT`。
  - 产物：`solve_reports/m48_hi4_run.out.log`、`m48_hi4_run.err.log`
- m44（L8 依赖5字节，hi4 模型）全空间穷举：`2^36`，结果 `NO_HIT`。
  - 产物：`solve_reports/m44_hi4_run.out.log`、`m44_hi4_run.err.log`

3) 当前结论
- 在已验证模型下，`m40/m44/m48` 关键依赖空间已排空，仍未得到可通过输入。
- 继续盲目扩展长度将急剧增大成本（如 m56+），不适合在无新先验下直接硬跑。

4) 下次待办事项（优先级从高到低）
1. 动态断点取值：在 `__wcsnicmp` 调用前直接抓取参与比较的解密缓冲区前 5~16 字节，
   以“真值”反推输入约束，替代继续盲搜。
2. 逆向补全：围绕 `0x4021E0` 分支补充调用链（尤其是解密后字符串构造与长度来源），
   确认是否存在题目层面输入预处理差异（GUI 控件文本编码/截断/过滤）。
3. 约束求解升级：将现有 SMT 脚本改为“分段状态缓存 + 可恢复增量求解”，
   避免一次性构造超大数组导致长期 `unknown/timeout`。
4. 若仍无解：引入“Unicode 低字节精确注入”候选生成与 GUI 验证，系统化覆盖不可打印低字节输入场景。

六、本轮迭代进展（2026-04-17）
1) 已完成流程优化
- `sample_solver` 新增“时间截止 + 预算预留”机制：
  - 支持 `max_seconds` 截止控制（默认 6 小时），到期自动终止并写入 checkpoint。
  - 调整预算分配，避免早期阶段耗尽全部尝试次数，给后续 refine/focused 阶段保留预算。
- 新增两段增强搜索：
  - `coordinate_descent(seed_top_scored)`：对高分候选做坐标下降优化；
  - `focused_probe`：当已有 `3/5` 前缀命中时，对失配位做全字节定向枚举。
- `pipeline` 新增环境变量开关：
  - `REVERSE_AGENT_SAMPLE_MAX_ATTEMPTS`（默认 250000）
  - `REVERSE_AGENT_SAMPLE_MAX_SECONDS`（默认 21600 秒，即 6 小时）

2) 已完成回归验证
- 测试：`python -m pytest -q` -> `43 passed`。
- 样本回归：`samplereverse.exe`（预算 900000，截止 1800s）仍未命中最终可通过输入，返回 `NOT_FOUND`。

3) 新证据与状态
- 已出现更强局部命中：`best_score=3/5`，`best_mask=11010`。
- 最新 checkpoint：
  - `resume_len=5`
  - `resume_idx=10031393`
  - `dep_index=2259846`
  - `m44_index=3416881`
  - `m48_index=3416881`
  - `best_hex=464e5a46004141`

4) 当前结论
- 本轮优化已显著提升“可持续迭代能力”（可控终止、可恢复推进、定向精修），但目标样本仍未解出。
- 后续最高优先级仍是“动态断点取真值”（比较前缓冲区直接抓取），否则仅靠前缀分数搜索仍可能长期停留在 3/5 附近。

七、补充修复（2026-04-17）
1) 已修复 null 字节导致的 Copilot CLI 调用崩溃
- 现象：当候选中包含 `\x00` 时，prompt 直接拼接会触发 `ValueError: embedded null character`。
- 修复：`pipeline.build_prompt` 对候选/证据/字符串样本做控制字符转义（`\x00` -> `\\x00`）。
- 回归：新增测试 `test_build_prompt_escapes_null_bytes_in_candidates`；当前测试 `45 passed`。

2) “6 小时终止”升级为跨续跑累计生效
- `sample_solver` checkpoint 新增 `started_epoch/deadline_epoch`，后续续跑按同一截止时间继续，不会每次重置计时。
- 在 `REVERSE_AGENT_SAMPLE_MAX_SECONDS=30` 的快速验证下，样本可立即走到 deadline 分支并安全收尾（无崩溃）。

3) 新增“全流程硬截止”语义（pipeline）
- 当 `SampleProbe` 证据出现 `runtime_probe:deadline_reached=1` 时：
  - 跳过 `angr` 后备求解
  - 跳过模型调用
  - 跳过运行时校验
  - 直接按 `NOT_FOUND` 收尾并生成报告
- 目的：严格满足“6小时后终止任务”，避免截止后仍消耗额外计算与等待。
- 回归：新增测试 `test_sample_deadline_hard_stop_skips_angr_and_model`，验证 deadline 命中后确实跳过 angr 与模型调用。

八、持续迭代补强（2026-04-17）
1) 已修复“deadline 被旧配置永久锁死”的问题
- 新增 checkpoint 字段：`deadline_budget_seconds`。
- 逻辑：当 `REVERSE_AGENT_SAMPLE_MAX_SECONDS` 与 checkpoint 记录的预算秒数不一致时，自动重置该样本的 `started_epoch/deadline_epoch` 窗口。
- 效果：可在不同轮次灵活调整预算（如 30s 快速试跑后，再切回 1800s/21600s 正常迭代）。

2) 回归测试与当前状态
- 新增测试：`test_samplereverse_deadline_budget_change_resets_window`。
- 当前测试：`python -m pytest -q` -> `46 passed`。
- 样本 `samplereverse.exe` 当前仍未解出，流程可持续迭代且具备可控终止与可恢复推进能力。

九、迭代补强（2026-04-17，第二轮）
1) 已完成搜索目标函数升级与 Beam 扩展
- `sample_solver` 新增 checkpoint 字段：`best_distance`（与 `best_score/best_mask` 一起做最优态持久化）。
- `best_update` 判定新增距离维度（同分同掩码时优先更接近 `flag{` 的解密前缀）。
- 新增 `beam_probe` 阶段：基于 typable 候选进行单字节邻域扩展，目标函数为
  `objective=(score, mask, -distance)`，用于突破 3/5 局部最优。
- stochastic 阶段统一复用同一 objective，减少阶段间排序标准不一致。

2) 回归与样本验证
- 新增测试：`test_objective_prefers_smaller_distance_on_tie`。
- 当前测试：`python -m pytest -q` -> `47 passed`。
- 样本回归（`max_attempts=900000`, `max_seconds=1800`）结果：仍 `NOT_FOUND`。
- 本轮 checkpoint：
  - `resume_len=5`
  - `resume_idx=20639028`
  - `best_score=3/5`
  - `best_mask=10110`
  - `best_distance=237`
  - `best_hex=61576168212121`

十、迭代补强（2026-04-17，第三轮）
1) 已补齐更长长度的依赖探测窗口
- 在 `sample_solver` 新增两段依赖探测：
  - `L10/L11(prefix6), m56/m60`
  - 分别用 `prefix6 + "AAAA"`（长度 10）和 `prefix6 + "AAAAA"`（长度 11）进入同一前缀命中目标函数。
- 新增 checkpoint 字段：
  - `m56_tier/m56_index`
  - `m60_tier/m60_index`
  用于跨续跑持续推进，不重复扫已覆盖分片。

2) 预算重平衡（确保新阶段真实执行）
- 将旧阶段预算上限下调，使 `L10/L11` 阶段在常规预算下可稳定获得执行窗口：
  - `dep_budget` 320000 -> 140000
  - `hi_budget` 420000 -> 180000
  - `hi2_budget` 520000 -> 220000
- 验证证据已出现：
  - `runtime_probe:dependency_probe=L10/L11(prefix6),m56/m60`

3) 回归与样本状态
- 测试：`python -m pytest -q` -> `47 passed`。
- 样本回归（500000/1800）结果：仍 `NOT_FOUND`。
- 最新 checkpoint（已进入新窗口）：
  - `resume_idx=25957523`
  - `m56_index=27392`
  - `m60_index=27393`
  - `best_score=3/5`
  - `best_mask=10011`
  - `best_distance=192`
  - `best_hex=6a534f33212121`

十一、迭代补强（2026-04-17，第四轮）
1) 已扩展 stochastic 阶段搜索域
- 增加长度突变操作（插入/删除），避免仅在固定长度局部最优停滞。
- 增加全字节突变分支（1..255）与可打印字符分支混合，降低“仅 printable 域”偏差。
- 维持评分目标 `objective=(score, mask, -distance)`，与 beam/全局 best 一致。

2) 回归状态
- 测试：`python -m pytest -q` -> `47 passed`。
- 样本单轮回归（700000/1800）：仍 `NOT_FOUND`，但断点继续推进到更长依赖窗口（`m56/m60`）。
- 已清理旧 detached 续跑进程冲突，重启单一 clean runner，避免 checkpoint/schema 被旧进程覆盖。

十二、迭代补强（2026-04-17，第五轮）
1) 已移除“可输入字符”硬限制，恢复全字节搜索能力
- 保留历史 `best_hex`（即使包含控制字符），不再因 non-typable 直接清零最优态。
- `beam_probe` 不再过滤 non-typable seed，允许从高分控制字符候选继续扩展。
- `focused_probe` 的突变字符集从 printable 扩展为 `0x01..0xFF`（仅排除 `0x00` 终止符）。

2) 回归结果
- 测试：`python -m pytest -q` -> `47 passed`。
- 样本单轮回归（700000/1800）：仍 `NOT_FOUND`，但最优态回到 `3/5`，并出现控制字符候选分支持续被探索。

十三、迭代补强（2026-04-17，第六轮）
1) 已补齐更深依赖窗口
- 新增 `L12/L13(prefix7)` 依赖探测（m64/m68），并新增断点字段：
  - `m64_tier/m64_index`
  - `m68_tier/m68_index`
- 对四层依赖探测预算重新平衡，确保在中等预算（如 500000）下四层都能执行。

2) 回归状态
- 测试：`python -m pytest -q` -> `47 passed`。
- 样本回归（500000/1800）：仍 `NOT_FOUND`，但 checkpoint 已进入 `m64/m68` 空间：
  - `m64_index=17392`
  - `m68_index=17393`
  - `best_score=3/5`

十四、迭代补强（2026-04-17，第七轮）
1) 已引入后段探索增强与可观测性
- 新增 `anneal_probe`（多起点退火）并在日志中记录阶段状态：
  - `runtime_probe:stages stochastic=... anneal=...`
- 预算重平衡后，长跑日志已确认 `stochastic=1 anneal=1` 持续执行。

2) 已支持随机种子配置
- `pipeline` 新增环境变量：`REVERSE_AGENT_SAMPLE_RANDOM_SEED`（默认 1337），用于控制 stochastic/anneal 随机轨迹。
- 便于后续并行多 seed 运行，减少单轨迹局部最优锁定。

3) 当前样本状态
- 测试：`python -m pytest -q` -> `47 passed`。
- 长跑仍未命中最终输入，但断点持续推进（`resume_idx` 与 `m64/m68` 索引持续增长）。

十五、迭代补强（2026-04-17，第八轮）
1) 已新增三字节联动精修（tri_probe）与阶段可观测性
- 在 focused 两失配场景下，新增 support-byte 扰动 + 双失配重扫（`tri_probe`），用于突破固定两位穷举的局部极值。
- 阶段状态统一输出为：
  - `runtime_probe:stages tri=<0/1> stochastic=<0/1> anneal=<0/1>`
  便于长跑日志直接判断后段是否真实执行。

2) 预算与执行状态
- 当前回归（500000）下，`anneal` 可稳定执行（`anneal=1`），`stochastic/tri` 在部分轮次会因前段最优态门限未满足而不触发。
- 仍保持 `47 passed`，样本未命中最终输入。

十六、并行迭代状态（2026-04-17）
1) 已同时运行多轨迹长跑
- 主轨迹：`solve_reports\\samplereverse_longrun.log`（共享主 checkpoint）。
- 独立 seed 轨迹：
  - `solve_reports\\samplereverse_longrun_seed_2027.log`
  - `solve_reports\\samplereverse_longrun_seed_8123.log`
- 各 seed 使用独立 `tool_artifacts\\samplereverse_seed_<seed>`，避免 checkpoint 互相覆盖。

2) 当前观测
- 多轨迹均已进入 `tri=1 stochastic=1 anneal=1` 的后段执行状态（视轮次而定）。
- 尚未出现 `runtime_candidate:*` 命中记录，继续推进。

十七、动态断点提取实现进展（2026-04-17，第九轮）
1) 已完成核心内存提取脚本
- 新增 `reverse_agent/olly_scripts/extract_decrypted_buffer_memory.py`：
  - 使用 Windows API (ctypes) 进行进程内存读取
  - 支持进程名查找和内存模式搜索
  - 实现 UTF-16LE 宽字符串提取
  - 自动搜索 "flag{" 模式（UTF-16LE 编码）
  - 支持 psutil 依赖检查和优雅降级
- 脚本支持参数：
  - `--target`: 目标可执行文件路径
  - `--out`: JSON 输出路径
  - `--dummy-input`: 触发解密的虚拟输入（默认 "AAAAAAA"）
  - `--timeout`: 超时时间（默认 10 秒）

2) 下一次工作内容（按优先级排序）
1. 管道集成（1 小时）：
   - 在 `pipeline.py` 添加 `_extract_breakpoint_value_memory()` 函数
   - 添加 `BreakpointExtractionResult` 数据类
   - 在 `run_pipeline()` 约 760 行处集成内存提取阶段
   - 确保成功时将提取的缓冲区作为高优先级候选
   - 失败时回退到现有搜索策略

2. 增强启发式分析（1 小时）：
   - 在 `sample_solver.py` 添加 `_analyze_rc4_constraints()` 函数
   - 添加 `_generate_constraint_aware_candidates()` 函数
   - 基于已知 RC4 约束生成增强候选
   - 集成 UTF-16LE 和 Base64 编码变体

3. 测试与验证（1-2 小时）：
   - 创建 `tests/test_memory_extraction.py` 单元测试
   - 创建 `tests/test_breakpoint_integration.py` 集成测试
   - 使用实际 samplereverse.exe 进行端到端测试
   - 验证失败场景的回退行为

3) 预期效果
- 成功路径：将 2500+ 万次搜索尝试缩减为单次运行时探测（1-2 分钟）
- 回退路径：即使内存提取失败，通过增强的 RC4 约束分析提高搜索效率
- 完全向后兼容：不影响现有功能和测试套件

十八、阶段性收尾（2026-04-18，按“半小时未解出则终止并更新状态”执行）
1) 本轮已落地的工程增强
- `pipeline` 已新增 GUI 运行时证据采集：
  - 命中 `samplereverse` 特征且为 GUI EXE 时，会额外记录
    `runtime_gui:title=...`
    `runtime_gui:controls=...`
    `runtime_gui:probe_input=...`
    `runtime_gui:probe_output=...`
  - 已在真实样本上验证可稳定采集：
    `输入的密钥是 : <candidate>`
    `尝试利用该密钥进行解密`
    `密钥不正确`
- `pipeline` 已新增低字节 GUI 注入映射：
  - 对非打印字节使用保低字节 Unicode 映射（如 `0x01 -> U+0101`），
    使控制字节候选可真实送入 GUI 控件，不再因 `Edit` 控件吞字节而失真。
- `sample_solver` 已新增高分候选回传：
  - `top_scored` 候选现在会回流到主流程，进入统一 GUI 校验链路。
- 新增 `reverse_agent/samplereverse_z3.py`：
  - 提供样本专用 Z3 分区求解入口；
  - 由环境变量 `REVERSE_AGENT_SAMPLE_ENABLE_Z3` 控制是否启用。

2) 已完成的样本级验证
- 测试基线已更新为：`python -m pytest -q` -> `50 passed`。
- 真实样本 GUI 运行时探针已确认成功：
  - 窗口标题：`CTF`
  - 关键控件：`1000(button) / 1001(edit) / 1002(edit)`
- 当前主 checkpoint `best_hex=455f7d39010101` 已通过新注入路径真实送入样本；
  程序回显：`输入的密钥是 : E_}9āāā`，说明低字节注入链路已生效。

3) 本轮高价值搜索与结论
- `patched` 样本已核对：与原 `samplereverse.exe` 二进制一致，不存在额外补丁差异。
- 对长窗口做了新的结构化搜索与局部穷举：
  - `m56` 家族得到稳定 `3/5` 候选：
    - 候选：`720df805616041414141`
    - 前缀：`664c218f7b` -> `f l ? ? {`
    - 掩码：`11001`
  - `m60` 家族局部搜索得到另一条 `3/5` 候选：
    - 候选：`5542f10a7b764141414141`
    - 前缀：`466c4142d8` -> `f l a ? ?`
    - 掩码：`11100`
- 已对上述候选分别执行：
  - 两字节全枚举邻域搜索；
  - pairwise hill-climb；
  - 针对 `m60 fla??` 家族的三字节定向 C 穷举（`solve_reports/triple_search_m60_family.c/.exe`）。
- 结果：
  - 均未产生 `5/5` 命中；
  - 本轮结束时仍未得到可通过输入。

4) 当前结论
- 本项目在“样本真实验证能力”和“控制字节候选可验证性”上已明显强于上一轮。
- 但就 `samplereverse.exe` 本题而言，单纯依赖启发式/局部搜索继续推进，性价比已经明显下降。
- 若后续继续攻关，优先级应调整为：
1. compare 前真值提取（断点/调试器/更强内存观察），直接拿到参与比较的宽字符串；
2. 基于当前 `m56/m60` 高分家族做更强约束求解，而不是继续泛化随机搜索；
3. 仅在获得新先验后再扩展更深长度窗口。

十九、交接收尾与最新主线（2026-04-21）
1) 平台状态
- `samplereverse` 已迁入 profile 主线：
  - `reverse_agent\profiles\samplereverse.py`
  - `reverse_agent\strategies\compare_aware_search.py`
  - `reverse_agent\transforms\samplereverse.py`
- `CompareAwareSearchStrategy` 已成为该样本的主策略，`sample_solver.py` 仅保留为回退链。
- `CompareProbe` 现已统一产出 offline/runtime compare-aware 指标：
  - `offline_ci_exact_wchars`
  - `offline_ci_distance5`
  - `offline_raw_prefix_hex`
  - `runtime_ci_exact_wchars`
  - `runtime_ci_distance5`
  - `runtime_lhs_prefix_hex_10`
  - `compare_semantics_agree`

2) 当前最强样本进展
- 当前主线长度固定为：`L15(prefix8)`。
- 当前最强实机一致 exact2 候选：
  - `78d540b49c59077041414141414141`
  - 离线前缀：`46006c004464830d311c`
  - 运行时前缀：`46006c004464830d311c`
  - 指标：`runtime_ci_exact_wchars=2`，`runtime_ci_distance5=246`
- 旧 exact2 对照候选：
  - `4a78f0eaeb4f13b041414141414141`
  - 运行时前缀：`46004c007e40b92886f5`
  - 指标：`runtime_ci_exact_wchars=2`，`runtime_ci_distance5=471`
- 当前测试基线：`python -m pytest -q` -> `79 passed`。

3) 已验证结论
- `L15` 前 10 compare 字节只依赖 `prefix8`；后 7 字节固定为 `AAAAAAA` 时，不影响这 10 字节。
- 旧 exact2 盆地（`4a78...`）与新 exact2 盆地（`78d5...`）的 pair 邻域都较刚性，未直接打出 exact3。
- 新 exact1 frontier 已被 `CompareProbe` 证实为实机一致盆地：
  - `95a3f65dcedb629041414141414141`
  - 运行时前缀：`6600583a481ab842862c`
  - 指标：`runtime_ci_exact_wchars=1`，`runtime_ci_distance5=305`
- 经过 strata-aware source 采样后，搜索已不再只围绕旧 exact2 anchor 自转，能够稳定保留 exact2 与 exact1 两类前沿状态。

4) 新对话下一步
- 不要回到 `L13/L14/L16` 主线，也不要默认回退到旧 `sample_solver` 盲搜。
- 新对话必须从以下 artifact 开始：
  - `solve_reports\tool_artifacts\samplereverse_compare_aware_long4_strata\samplereverse_compare_aware_result.json`
  - `solve_reports\tool_artifacts\samplereverse_compare_aware_long4_strata\samplereverse_compare_aware_compare_1.json`
  - `solve_reports\tool_artifacts\samplereverse_compare_aware_long5_newbest\samplereverse_compare_aware_result.json`
  - `solve_reports\tool_artifacts\samplereverse_compare_aware_pairscan_newbest_exact2\pairscan_summary.json`
- 新对话的优先顺序固定为：
  1. 围绕新 exact2 盆地 `78d540b49c590770` 做 compare-aware bridge 搜索（triad/quartet/quint）。
  2. 若 bridge 无进展，再做 strata 长跑，并同时导出总榜与分层榜。
  3. 只有前两步都无新进展时，才做围绕新 exact2 anchor 的定向 SMT。

二十、bridge-first 实跑收口与 compare 前真值扩展（2026-04-21）
1) 真实样本 bridge-first 主线已完整执行
- 真实样本：
  - `E:\xwechat_files\wxid_9ky6h8wz58b912_a1e5\msg\file\2026-04\samplereverse.exe`
- 本轮主线 artifact：
  - `solve_reports\tool_artifacts\samplereverse_compare_aware_bridge_refresh_20260421\run_summary.json`
  - `solve_reports\tool_artifacts\samplereverse_compare_aware_bridge_refresh_20260421\bridge\pairscan_summary.json`
  - `solve_reports\tool_artifacts\samplereverse_compare_aware_bridge_refresh_20260421\bridge\bridge_search_result.json`
  - `solve_reports\tool_artifacts\samplereverse_compare_aware_bridge_refresh_20260421\bridge\bridge_validation\bridge_validation.json`
  - `solve_reports\tool_artifacts\samplereverse_compare_aware_bridge_refresh_20260421\samplereverse_compare_aware_result.json`
  - `solve_reports\tool_artifacts\samplereverse_compare_aware_bridge_refresh_20260421\samplereverse_compare_aware_strata_summary.json`
- 主 anchor：
  - `78d540b49c590770`
- 执行顺序已验证为：
  - `pairscan -> triad/quartet/quint -> bridge validation -> refine -> SMT`

2) 本轮真实样本结果
- bridge 未超过旧基线：
  - `bridge_validation` 最优仍为 `78d540b49c59077041414141414141`
  - `runtime_ci_exact_wchars=2`
  - `runtime_ci_distance5=246`
  - `compare_semantics_agree=true`
- refine 未超过 bridge：
  - `samplereverse_compare_aware_result.json` 最优仍为同一 exact2 候选
  - `strata_summary` 中 `bridge_promoted` 为空，`refine_anchors` 仅保留 `78d540b49c590770` 与 `95a3f65dcedb6290`
- SMT 仅复现旧盆地：
  - `completed_stage=smt`
  - 未出现 `runtime_ci_exact_wchars >= 3`
  - 未出现 `runtime_ci_distance5 < 246`

3) 停滞位置判断
- 停滞首先发生在 bridge：
  - `pairscan` 热点位只稳定收敛到 `[0, 1]`
  - 未形成可用 `hot_nibbles`
  - 无 bridge promoted anchor
- refine 和 SMT 只是复现旧 exact2 盆地，没有产生新的 runtime 改善。
- 因此下一阶段不应继续加长跑预算，优先级切换为 compare 前真值提取。

4) compare 前真值扩展已落地
- `reverse_agent\olly_scripts\compare_probe.py` 现新增并统一输出：
  - `runtime_lhs_prefix_hex_16`
  - `runtime_lhs_prefix_bytes_captured`
- `RuntimeCompareEvidence` 与 compare-aware validation 已同步携带这些字段，不再只保留前 10 字节摘要。
- `SamplereverseProfile.collect_runtime_probes()` 已对 `run_compare_probe()` 显式启用 16 字节 capture。
- 当前测试基线：
  - `python -m pytest -q` -> `81 passed`

5) 新真值 artifact
- 已对当前最优 exact2 候选执行扩展 CompareProbe：
  - `solve_reports\tool_artifacts\samplereverse_compare_truth_refresh_20260421\compare_probe_exact2_anchor.json`
- 结果：
  - `runtime_lhs_prefix_hex_10=46006c004464830d311c`
  - `runtime_lhs_prefix_hex_16=46006c004464830d311c701038525b85`
  - `runtime_lhs_prefix_bytes_captured=16`
  - `runtime_ci_exact_wchars=2`
  - `runtime_ci_distance5=246`
  - `compare_semantics_agree=true`

6) 下一对话固定起点
- 不再继续扩 bridge/refine 预算来碰运气。
- 下一步优先做：
  1. 围绕 `runtime_lhs_prefix_hex_16` 增加 compare 前真值对齐分析，确认第 6 到 8 个字节是否能反推出新的 bridge 热点位。
  2. 若 extended runtime prefix 暴露出稳定结构偏差，再回到 `compare_aware_search.py` 修正热点位提取或局部评分。
  3. 只有在 extended truth 仍无法提供额外约束时，才继续收缩到单 anchor 的局部 SMT。 

二十一、64 字节 compare 真值与局部盆地复核（2026-04-21）
1) CompareProbe 已支持更长的 compare 前缓冲区抓取
- `reverse_agent\olly_scripts\compare_probe.py` 现已真正按 `--capture-prefix-bytes` 放宽 Frida 读取长度，不再只是输出层截断参数。
- 新增统一字段：
  - `runtime_lhs_prefix_hex`
  - `runtime_lhs_prefix_bytes_captured`
- 兼容字段仍保留：
  - `runtime_lhs_prefix_hex_10`
  - `runtime_lhs_prefix_hex_16`
- 回归：
  - `python -m pytest -q` -> `81 passed`

2) 当前最优 exact2 anchor 的 64 字节运行时/离线一致性
- 真实样本 artifact：
  - `solve_reports\tool_artifacts\samplereverse_compare_truth_refresh_20260421\compare_probe_exact2_anchor_64b.json`
- 候选：
  - `78d540b49c59077041414141414141`
- 结果：
  - `runtime_lhs_prefix_bytes_captured=64`
  - `runtime_lhs_prefix_hex == lhs_wide_hex`
  - 运行时 64 字节：
    - `46006c004464830d311c701038525b853072ee18c26f2b523688e43ab9670038ac3d9da73cee402b95cf8934602bfb99ba4173702e4f23659def4313aa338f64`
  - 离线 `_decrypt_prefix(candidate, 64)` 与上面完全一致。
- 结论：
  - 当前 transform/RC4 模型不仅在前 10 或 16 字节上有效，在当前 exact2 盆地上对前 64 字节也与实机一致。
  - 因此后续优化重点不再是“怀疑模型失真”，而是“如何让搜索目标利用更长窗口结构”。

3) 局部盆地复核
- 对主 anchor `78d540b49c590770` 做了两类离线组合实验：
  - 前 5 个位置高价值单字节池组合；
  - 全 8 个位置高价值单字节池组合（`4^8 = 390625`）。
- 结果：
  - 两类实验都未超过当前最优 `runtime_ci_exact_wchars=2 / ci_distance5=246` 对应的离线盆地。
  - 说明“仅靠当前 exact2 anchor 周围的局部单字节高价值组合”不足以打出 `exact3`。

4) 历史长窗口家族的真实样本复核
- 本轮已对以下历史候选执行 CompareProbe：
  - `m56_family = 720df805616041414141`
  - `m60_family = 5542f10a7b764141414141`
  - `legacy_best = 455f7d39010101`
- 结果均不优于 L15 主线：
  - `m56_family` -> `runtime_ci_exact_wchars=0`, `runtime_ci_distance5=620`
  - `m60_family` -> `runtime_ci_exact_wchars=0`, `runtime_ci_distance5=1042`
  - `legacy_best` -> `runtime_ci_exact_wchars=0`, `runtime_ci_distance5=617`
- 结论：
  - 旧 `m56/m60` 家族不足以推翻当前 `L15(prefix8)` 主线假设。
  - 下一轮不要把精力切回旧长度家族盲搜。

5) 下一轮固定方向
- 可以做：
  1. 在 `compare_aware_search.py` 中引入“长窗口结构目标”，让排序/引导阶段消费 `runtime_lhs_prefix_hex` 或离线 64 字节结构，而不只看前 10 字节。
  2. 在保持 `L15(prefix8)` 的前提下，尝试更强的约束式 prefix8 搜索，而不是继续只做局部 pair/triad 邻域。
- 不建议做：
 1. 继续单纯增加 bridge/refine 预算；
 2. 回退到旧 `sample_solver` 全局盲搜；
 3. 重新主攻 `m56/m60` 旧长度窗口。 

二十二、长窗口评分与 guided pool 主线落地（2026-04-21）
1) 代码实现已完成
- 关键改动：
  - `reverse_agent\transforms\samplereverse.py`
  - `reverse_agent\strategies\compare_aware_search.py`
  - `reverse_agent\profiles\samplereverse.py`
  - `tests\test_compare_aware_search_strategy.py`
- 已落地内容：
  - `score_prefix()` 新增长窗口结构指标：
    - `raw_prefix_hex_64`
    - `wide_ascii_contiguous_16`
    - `wide_ascii_total_16`
    - `wide_zero_high_pairs_16`
    - `flaglike_tail_pairs_16`
  - compare-aware 主线阶段顺序改为：
    - `bridge -> guided_pool -> refine -> smt`
  - `SamplereverseProfile.collect_runtime_probes()` 默认已改为 `capture_prefix_bytes=64`
  - 新 artifact：
    - `samplereverse_compare_aware_guided_pool_result.json`
    - `samplereverse_compare_aware_guided_pool_validation.json`

2) 自动化回归
- 当前测试基线：
  - `python -m pytest -q` -> `85 passed`

3) 64 字节 recheck
- 新 artifact：
  - `solve_reports\tool_artifacts\samplereverse_compare_truth_refresh_20260421\compare_probe_exact2_anchor_64b_recheck.json`
- 候选：
  - `78d540b49c59077041414141414141`
- 结果：
  - `runtime_lhs_prefix_bytes_captured=64`
  - `runtime_ci_exact_wchars=2`
  - `runtime_ci_distance5=246`
  - 运行时 `runtime_lhs_prefix_hex` 与离线 `_decrypt_prefix(candidate, 64)` 完全一致

4) 真实样本新主线回归
- artifact 目录：
  - `solve_reports\tool_artifacts\samplereverse_compare_aware_guided_pool_refresh_20260421`
- 阶段执行情况：
  - `bridge` 最优仍为主 anchor `78d540b49c59077041414141414141`
  - `guided_pool` 已真实执行，覆盖位置 `[0,1,2,3,4]`，每位小池 10 个值，beam 上限 16
  - `refine` 与 `smt` 均已执行；`smt` 返回 `unknown`
- 当前最优：
  - `best bridge = 78d540b49c59077041414141414141`
  - `best refine = 78d540b49c59077041414141414141`
  - `best smt = none`
  - `compare_semantics_agree = true`
  - `runtime_ci_exact_wchars = 2`
  - `runtime_ci_distance5 = 246`
- 结论：
  - 本轮没有超过基线 `246`
  - 停滞点已可明确归因到：
    - `bridge` 未突破旧 exact2 盆地
    - `guided_pool` 也未筛出优于主 anchor 的 runtime 结果
    - `refine` 复现旧盆地
    - `smt` 未得到可用候选

5) 下一轮固定方向
- 应优先做：
  1. 让 long-window 结构目标不只做 tie-break，而是进入更强约束或更强筛选；
  2. 基于当前 `guided_pool` value pool 和 64 字节真值，分析第 0 到 4 字节是否存在更有信息量的组合/约束方式；
  3. 若继续使用 SMT，优先把长窗口结构目标并入求解后筛选，而不是继续扩大变量位。
- 不建议做：
  1. 继续单纯放大 `guided_pool` / `refine` 预算；
  2. 回退到旧 `sample_solver` 盲搜；
  3. 重新切回 `m56/m60` 长度主线。 

2026-04-21 继续迭代：exploratory guided pool

1) 本轮代码调整
- 文件：
  - `reverse_agent/strategies/compare_aware_search.py`
  - `tests/test_compare_aware_search_strategy.py`
- 变更：
  - `guided_pool` 的 beam 保留策略从“只保 exact_floor 命中项”改为“双轨保留”：
    - 先保 `ci_exact_wchars >= exact_floor` 的主线候选；
    - 再追加少量 compare-aware 排序最优的探索候选，默认 `exploration_slots=4`。
  - guided pool artifact 新增：
    - `exploration_slots`
    - `stage_stats`
    - 每一层的 `primary_kept` / `exploratory_kept` / `floor_matched` / `beam_size`
  - 新增单测，确认 beam 会保留有限探索尾部，而不会重新退化成盲搜。

2) 自动化回归
- 当前测试基线：
  - `python -m pytest -q` -> `86 passed`

3) 真实样本回归
- artifact 目录：
  - `solve_reports\tool_artifacts\samplereverse_compare_aware_exploratory_pool_refresh_20260421`
- 阶段执行情况：
  - `bridge` 仍停在主 anchor `78d540b49c59077041414141414141`
  - `guided_pool` 已不再塌成 beam=1，而是稳定为：
    - 每层 `primary=1`
    - 每层 `explore=4`
    - 每层 `beam=5`
  - `guided_pool` Top 5 候选已进入真实 CompareProbe 校验
  - `refine` 与 `smt` 仍然执行；`smt` 继续返回 `unknown`
- 当前最优：
  - `best bridge = 78d540b49c59077041414141414141`
  - `best guided_pool = 78d540b49c59077041414141414141`
  - `best refine = 78d540b49c59077041414141414141`
  - `best smt = none`
  - `compare_semantics_agree = true`
  - `runtime_ci_exact_wchars = 2`
  - `runtime_ci_distance5 = 246`
- 探索候选实机结果：
  - `788940b49c590770...` -> `runtime_ci_distance5 = 293`
  - `78d540c79c590770...` -> `runtime_ci_distance5 = 346`
  - `3e0940b49c590770...` -> `runtime_ci_distance5 = 408`
  - `780940b49c590770...` -> `runtime_ci_distance5 = 424`

4) 本轮结论
- 这轮已经证伪“问题只是 beam 过窄”这个假设：
  - beam 现在已稳定展开；
  - 探索候选也已进入真实 compare；
  - 但仍没有任何候选把 `runtime_ci_distance5` 压到 `246` 以下。
- 因此当前停滞点不是 guided pool 没展开，而是：
  - 第 0 到 4 字节的小池组合本身没有导向更优盆地；
  - 继续单纯扩 pool/beam 预算的收益预计很低。

5) 下一轮固定方向
- 应优先做：
  1. 把 long-window 结构目标从 tie-break 提升为更强筛选或约束；
  2. 利用现有 `stage_stats` 和 `position_profiles`，重新设计 position/value 组合规则，而不是继续均匀组合；
  3. 若继续使用 SMT，优先把这些结构目标并入目标函数或候选过滤。
- 不建议做：
  1. 继续只加 `guided_pool` 的 `beam_limit` 或 `exploration_slots`；
  2. 回退到 blind brute force；
  3. 重新开放旧长度主线。

2026-04-21 继续迭代：frontier validation + frontier anchors

1) 本轮代码调整
- 文件：
  - `reverse_agent/strategies/compare_aware_search.py`
  - `tests/test_compare_aware_search_strategy.py`
- 变更：
  - 新增 `_diverse_validation_candidates()`，final validation 不再只盯 `ci_exact_wchars>=2` 主盆地，而是显式混入：
    - best overall
    - best exact2
    - best exact1
    - best exact0
    - best distance / best raw-distance frontiers
  - `CompareAwareSearchStrategy.run()` 在 refine 后会把 `guided + bridge + refine` 的跨阶段 frontier 写入 `validation_candidates`，再统一跑 CompareProbe。
  - `guided_pool` 的 `exact_floor` 调整为跟随 base exact：
    - exact2 base 仍保守；
    - exact0 base 允许 `exact_floor=0`，避免 frontier basin 被提前截断。
  - 新增单测，确认多样化 validation frontier 会保留跨盆地候选。

2) 自动化回归
- 当前测试基线：
  - `python -m pytest -q` -> `87 passed`

3) 真实样本主线回归
- artifact 目录：
  - `solve_reports\tool_artifacts\samplereverse_compare_aware_frontier_validation_refresh_20260421`
- 结果：
  - 主 anchor `78d540b49c590770...` 仍是最强 exact2，`runtime_ci_distance5=246`
  - 但 final validation 已首次把低 exact / 低 distance frontier 候选送入真实 CompareProbe
- 关键新盆地：
  - `f649b64b5e97dbd0...` -> `runtime_ci_exact_wchars=0`, `runtime_ci_distance5=218`, `compare_semantics_agree=true`
  - `0d4e75f7e9609a10...` -> `runtime_ci_distance5=227`, `compare_semantics_agree=true`
  - `7526c36cd2e52b60...` -> `runtime_ci_distance5=238`, `compare_semantics_agree=true`
  - `e80c7471d342f6f0...` 离线 `174`，但运行时 `242` 且 `compare_semantics_agree=false`
- 结论：
  - compare-aware 主线不再只有一个 exact2 盆地；
  - 已确认存在更低 runtime distance 的 compare-agree exact0 frontier。

4) frontier anchor 定向实验
- artifact 目录：
  - `solve_reports\tool_artifacts\samplereverse_frontier_anchor_probe_20260421`
  - `solve_reports\tool_artifacts\samplereverse_frontier_anchor_probe_floor0_20260421`
  - `solve_reports\tool_artifacts\samplereverse_refine_frontier_mix_20260421`
  - `solve_reports\tool_artifacts\samplereverse_refine_frontier_mix2_20260421`
  - `solve_reports\tool_artifacts\samplereverse_anchor_a47a_probe_20260421`
- 结果：
  - 把 `f649...` 混入 refine anchor 后，打出新的 compare-agree 更优盆地：
    - `a47a0a74bd353550...` -> `runtime_ci_distance5=208`, `compare_semantics_agree=true`
  - 围绕 `a47a...` 的 bridge / guided pool 没再低于 `208`
  - 对 `a47a...` 做 8 个字节位的单字节画像后确认：
    - 所有单字节最优都不优于 base 自身；
    - 当前 `208` 盆地在单字节层面已局部稳定。

5) 本轮结论
- 当前最优 runtime compare frontiers：
  - exact2 主盆地：`78d540b49c590770...` -> `246`
  - exact0 frontier：`a47a0a74bd353550...` -> `208`
- 这说明：
  - “低 exact 但低 distance”的 frontier 不是噪声，已经能稳定通过 compare-agree 校验；
  - 继续只围绕 exact2 盆地搜索会漏掉真实更优 basin；
  - 但 `a47a...` 和 `f649...` 两个 frontier 在当前单字节/局部 bridge 条件下都没有直接转成更高 exact。

6) 下一轮固定方向
- 应优先做：
  1. 把 `runtime_ci_distance5=208` 的 compare-agree frontier anchor 正式接入主线，而不是只靠手工实验；
  2. 让 guided/SMT 面向 frontier anchor 时使用“多字节联动”目标，而不是单字节画像；
  3. 对 `a47a...` 盆地优先做 pair/triad 式的 distance-driven 搜索，而不是 exact-driven bridge 排序。
- 不建议做：
  1. 再次只扩单字节小池；
  2. 再次只复跑 `78d540...` 主盆地；
  3. 把 `e80c...` 这类 `compare_semantics_agree=false` 的假前沿当成主突破点。

2026-04-21 继续迭代：frontier mainline implementation

1) 本轮代码调整
- 文件：
  - `reverse_agent/strategies/compare_aware_search.py`
  - `reverse_agent/samplereverse_z3.py`
  - `tests/test_compare_aware_search_strategy.py`
- 变更：
  - `CompareAwareSearchStrategy` 新增正式的 frontier anchor 选择逻辑：
    - `exact2_seed`
    - `exact1_frontier`
    - `exact0_frontier`
    - `distance_probe`
  - final validation 记录新增：
    - `offline_raw_distance10`
    - `frontier_role`
  - guided pool 新增 `anchor_mode` 分流：
    - `exact2` 保持现有 exact-aware 排序
    - `frontier` 改为 distance-driven 排序
  - frontier 模式新增 byte-pair profiling：
    - `pair_profiles`
    - `pair_stage_stats`
    - 以 pair profile 先产 seed，再进入 bounded beam 扩展
  - refine 主线新增 frontier-guided 与 frontier-refine 接力：
    - 主线先跑初始 refine
    - 再从 compare-agree frontier 自动提取 anchors
    - 再跑 frontier-guided
    - 再跑一轮 frontier-refine
  - SMT 新增 `anchor_mode`，frontier base 时改为优先最小化距离。

2) 自动化回归
- 当前测试基线：
  - `python -m pytest -q` -> `89 passed`

3) 真实样本回归
- artifact 目录：
  - `solve_reports\tool_artifacts\samplereverse_frontier_mainline_refresh_20260421`
  - `solve_reports\tool_artifacts\samplereverse_frontier_mainline_refresh2_20260421`
- 结果：
  - frontier anchors 已正式进入主线，不再需要手工回灌：
    - `95a3...` 会自动进入 frontier-guided
    - `f649...` 会自动进入 frontier-guided
    - `a47a...` 会自动出现在 final frontier summary 中
  - 当前最优仍未超过：
    - `a47a0a74bd353550...` -> `runtime_ci_distance5=208`, `compare_semantics_agree=true`
  - exact2 主盆地仍为：
    - `78d540b49c590770...` -> `runtime_ci_distance5=246`
- 结论：
  - “frontier anchor 主线化”已经生效；
  - 但本轮 mainline refresh 和 refresh2 都没有把 `208` 再压低；
  - 当前 `a47a...` frontier 已被稳定确认，但还没有被多字节搜索转成更高 exact。

4) 当前判断
- 现在主要瓶颈已经不是：
  - frontier 没进入主线
  - guided beam 太窄
  - exact0 basin 被 gate 截断
- 现在主要瓶颈是：
  - 当前 distance-driven pair-profile 还不够强，没把 `a47a...` 推到更低 distance；
  - current frontier mainline 仍然更擅长“发现更优 basin”，不擅长“在该 basin 内继续深挖”。

5) 下一轮固定方向
- 应优先做：
  1. 对 `a47a...` 引入更强的 pair/triad distance-driven排序与保留规则，不再只把 pair 当 seed；
  2. 让 frontier-guided 在第二轮 frontier-refine 产生新盆地后继续闭环一轮，而不是本轮止于 summary；
  3. 若继续尝试 SMT，优先围绕 `a47a...` 这类 compare-agree frontier 做局部多字节约束。
- 不建议做：
  1. 再次只放大 exact2 主盆地预算；
  2. 回退到 blind brute force；
  3. 把 `compare_semantics_agree=false` 的低 distance 假前沿当成主线。

2026-04-22 继续迭代：frontier closure verification

1) 本轮代码与测试收口
- 文件：
  - `reverse_agent/strategies/compare_aware_search.py`
  - `tests/test_compare_aware_search_strategy.py`
- 变更：
  - frontier-guided result/validation 统一带上：
    - `source_anchor`
    - `anchor_mode`
    - `anchor_lineage`
  - frontier 模式新增：
    - `pair_frontier_pool`
    - `triad_frontier_pool`
  - 主线新增：
    - `frontier_iterations`
    - `frontier_converged_reason`
    - 至多 2 轮 frontier 闭环
  - 自动化测试新增：
    - lineage 透传验证
    - frontier 改善时进入第二轮 guided 的控制流验证

2) 自动化回归
- 当前测试基线：
  - `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `19 passed`
  - `python -m pytest -q` -> `91 passed`

3) 真实样本闭环回归
- 真实样本路径：
  - `E:\xwechat_files\wxid_9ky6h8wz58b912_a1e5\msg\file\2026-04\samplereverse.exe`
- 新 artifact 目录：
  - `solve_reports\tool_artifacts\samplereverse_frontier_closure_refresh_20260422`
- 结果：
  - 新闭环主线已真实执行：
    - `bridge -> guided(seed) -> refine(seed) -> guided(frontier) -> refine(frontier) -> smt`
  - `frontier_converged_reason = distance_not_improved`
  - 第二轮 frontier-guided 未触发，原因不是控制流缺失，而是第一轮 frontier-refine 后没有产生更优 frontier
  - 当前 best exact2 runtime 仍为：
    - `78d540b49c590770...` -> `runtime_ci_exact_wchars=2`, `runtime_ci_distance5=246`
  - 当前 best frontier runtime 更新为：
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`, `compare_semantics_agree=true`
  - 旧的 `a47a... -> 208` 没有在这轮闭环里复现成 active frontier

4) 结论
- 本轮计划的“先验证新闭环”已经完成，且得到真实样本证据：
  - 闭环控制流是通的；
  - 停滞点现在可以由 `frontier_summary` / `strata_summary` 直接定位；
  - frontier 方向仍有收益，但当前更像是从 exact0 盆地切换到了 exact1 / `258` 盆地。
- 下一轮若继续推进，应优先围绕：
  - `5a3e7f46ddd474d0...`
  - 以及 `pair_frontier_pool` / `triad_frontier_pool`
  做 feedback 型 frontier-guided 或更强 frontier SMT，
  而不是重新扩大 exact2 主盆地预算。

## 2026-04-22 反馈式 Frontier Value Pool 实施

1) 代码实现
- 已在 `reverse_agent/strategies/compare_aware_search.py` 落地 feedback 型 frontier-guided：
  - 新增 frontier stage-local improvement gate：
    - `ci_distance5` 严格下降；
    - 或 `ci_distance5` 持平且 `raw_distance10` 严格下降；
    - 或 runtime `runtime_ci_distance5` 严格下降。
  - `pair_frontier_pool` / `triad_frontier_pool` 现在会标注 `improvement_gate_passed`
  - 新增 feedback value pool 聚合：
    - 从 improved pair/triad 候选提取高频字节值；
    - 回填到下一轮 frontier 的 `value_pools`
  - frontier refresh 改成优先使用 improved frontier lineage 作为下一轮 active frontier
  - frontier summary / strata summary 新增：
    - `improved_pair_frontier_pool_count`
    - `improved_triad_frontier_pool_count`
    - `feedback_value_pools`
    - `active_frontier_candidates`
    - `frontier_stall_stage`
  - SMT payload 现会记录 frontier feedback value pool 上下文

2) 自动化回归
- 新增测试覆盖：
  - improvement gate 的严格判定
  - improved pair/triad -> feedback value pool 聚合
  - frontier refresh 只晋升 improved frontier
  - SMT payload 会保留 feedback value pool
- 当前测试基线：
  - `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `23 passed`
  - `python -m pytest -q` -> `95 passed`

3) 真实样本回归
- 真实样本路径：
  - `E:\xwechat_files\wxid_9ky6h8wz58b912_a1e5\msg\file\2026-04\samplereverse.exe`
- 新 artifact 目录：
  - `solve_reports\tool_artifacts\samplereverse_frontier_feedback_refresh_20260422`
- 结果：
  - 新 feedback 主线已真实执行到：
    - `bridge -> guided(seed) -> refine(seed) -> guided(frontier) -> refine(frontier) -> smt`
  - `frontier_converged_reason = distance_not_improved`
  - `frontier_stall_stage = frontier_refine`
  - 当前 best exact2 runtime 仍为：
    - `78d540b49c590770...` -> `runtime_ci_exact_wchars=2`, `runtime_ci_distance5=246`
  - 当前 best frontier runtime 仍为：
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`
  - feedback 机制已真实改变第二条 frontier 的搜索空间：
    - `frontier_guided_1_788940...` 中出现
      - `improved_pair_frontier_pool_count = 1`
      - `improved_triad_frontier_pool_count = 1`
      - 非空 `feedback_sources` 与 `feedback_value_pools`
  - 但这些 improved pair/triad 仍未在 runtime validation / frontier refine 后形成更优 compare-agree frontier
  - `frontier_guided_1_5a3e...` 这条 exact1 frontier 上，improved pair/triad 计数仍为 0，说明当前停滞主要发生在 exact1 frontier 的局部深挖不足，而不是反馈控制流缺失

4) 结论
- 本轮计划已完成：
  - feedback 型 value pool 已进入主线；
  - improvement gate 已进入 frontier refresh；
  - 停滞点现在能被 artifact 直接定位。
- 当前最重要的新证据是：
  - feedback 机制本身已生效；
  - 但真正需要突破的是 `5a3e7f46ddd474d0...` 这条 exact1 frontier 在 `pair/triad -> validation -> refine` 之后仍没有 runtime 改善。
- 下一轮若继续推进，应优先考虑：
  - 提升 exact1 frontier 上 pair/triad 值池的质量，而不是再扩大 beam；
  - 或把 long-window / runtime compare 信号更强地接入 exact1 frontier 的保留规则。

## 2026-04-22 Exact1 Frontier Lane 实施

1) 代码实现
- 已在 `reverse_agent/strategies/compare_aware_search.py` 将 frontier lane 正式拆成 exact1 / exact0 两条内部子模式：
  - 新增 `frontier_submode`
    - `frontier_exact1`
    - `frontier_exact0`
  - exact1 lane 的 guided pool 改成：
    - 默认只放开前 `3-4` 个热点位
    - `exact_floor` 不再退回 `0`，而是保持 `>= 1`
    - value pool 以
      - base byte
      - small perturbation
      - exact1 pair/triad winning values
      为主，不再把单字节 profile 当成唯一主来源
  - exact1 lane 不再吸收 exact0 的 `incoming_feedback`
  - exact1 improvement gate 现在允许：
    - `ci_distance5` 持平
    - `raw_distance10` 改善
    - 且 `ci_exact_wchars` 不下降
  - frontier refine / refresh 现在按 active lane 隔离：
    - 有 exact1 improved frontier 时优先 exact1
    - 只有 exact1 没有 improved candidate 时才允许 exact0 接棒
  - frontier refine 的 top entry 上下文改成按 candidate anchor 回填，避免 exact2 候选被错误标成 exact1 lineage
  - SMT base 选择新增 exact1 优先规则，且 exact1 SMT 只消费 exact1 lineage 的 comparison entries / feedback pool

2) 自动化回归
- 新增测试覆盖：
  - `frontier_submode` 透传
  - exact1 improvement gate
  - exact1 value pool 不吸收 exact0 incoming feedback
  - SMT base 优先 exact1 frontier
- 当前测试基线：
  - `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `26 passed`
  - `python -m pytest -q` -> `98 passed`

3) 真实样本回归
- 真实样本路径：
  - `E:\xwechat_files\wxid_9ky6h8wz58b912_a1e5\msg\file\2026-04\samplereverse.exe`
- 新 run 目录：
  - `solve_reports\harness_runs\samplereverse_exact1_frontier_quality_20260422`
- 关键 artifact：
  - `solve_reports\harness_runs\samplereverse_exact1_frontier_quality_20260422\reports\tool_artifacts\samplereverse\samplereverse_compare_aware_frontier_summary.json`
  - `solve_reports\harness_runs\samplereverse_exact1_frontier_quality_20260422\reports\tool_artifacts\samplereverse\samplereverse_compare_aware_strata_summary.json`
  - `solve_reports\harness_runs\samplereverse_exact1_frontier_quality_20260422\reports\tool_artifacts\samplereverse\frontier_guided_1_5a3e7f46ddd474d0\samplereverse_compare_aware_guided_pool_result.json`
- 结果：
  - 新 lane 逻辑已真实执行，且 active frontier 明确变成：
    - `frontier_active_lane = frontier_exact1`
  - 当前收敛状态：
    - `frontier_converged_reason = distance_not_improved`
    - `frontier_stall_stage = pair_pool`
    - `frontier_exact1_stall_reason = distance_not_improved`
  - 当前 best exact2 runtime 仍为：
    - `78d540b49c590770...` -> `runtime_ci_exact_wchars=2`, `runtime_ci_distance5=246`
  - 当前 best exact1 frontier runtime 仍为：
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`
  - exact1 lane 的搜索空间已经明显变化：
    - `frontier_submode = frontier_exact1`
    - `positions = [0,1,2,3]`
    - `exact_floor = 1`
    - `feedback_value_pools` 已不再只是单字节 profile，示例：
      - pos0 -> `[0x5a, 0x59, 0x5b, 0x58, 0x5c, 0x33]`
  - 但这轮 exact1 lane 的 `pair/triad` 仍未产出 improved candidate：
    - `improved_pair_frontier_pool_count = 0`
    - `improved_triad_frontier_pool_count = 0`
  - 这说明瓶颈已经进一步收敛到：
    - exact1 lane 的 pair-level 组合质量
    - 而不是 beam、feedback 控制流或 refine/SMT 编排
- 备注：
  - harness 在模型阶段有一次 `gbk` 编码错误，但 compare-aware artifact 已完整落盘；本轮判断以这些 artifact 为准，不受该错误影响

4) 结论
- 本轮计划已完成，且关键假设已被真实样本验证：
  - exact1 / exact0 lane 分流是有效的；
  - exact1 不再被 exact0 feedback 污染；
  - 当前真正的停滞点已经从 `frontier_refine` 前移到了 `exact1 pair_pool`
- 下一轮若继续推进，优先级应改为：
  - 针对 `5a3e7f46ddd474d0...` 做更强的 exact1 pair-value 生成，而不是继续扩 beam
  - 或把 exact1 lane 的 pair ranking 再向 runtime/offline 长窗口结构信号倾斜

## 2026-04-22 Exact1 Locked Pair 生成实施

1) 代码实现
- 已在 `reverse_agent/strategies/compare_aware_search.py` 为 exact1 lane 加入 pair-level 定向生成：
  - 新增 exact1 pair locking：
    - `locked_pair_positions`
    - 默认只保留前 `2-3` 组 exact1 局部 pair
  - exact1 pair generation mode：
    - `pair_generation_mode = exact1_locked_pairs`
  - exact1 pair generation sources 会落盘：
    - `source_anchor_diff_positions`
    - `bridge_diff_positions`
    - `top_ranked_pairs`
  - exact1 pair soft gate 已加入：
    - `ci_exact_wchars` 从 `1 -> 0` 时默认丢弃
    - 只有 distance 改善足够大时才允许逃逸
  - 修复了 pair pool 的一个控制流问题：
    - 某一层 rank 全被 soft gate 过滤时，搜索不再提前中断，后续更深 rank 仍可继续尝试
  - guided pool artifact 现新增：
    - `locked_pair_positions`
    - `pair_generation_mode`
    - `pair_generation_sources`
    - `pair_drop_reasons`

2) 自动化回归
- 新增测试覆盖：
  - exact1 locked pair 只生成被锁定的 pair positions
  - exact1 soft gate 会过滤“降 exact 且无明显收益”的 pair
  - pair pool 深层 rank 不会因中间层被过滤而提前中断
- 当前测试基线：
  - `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `28 passed`
  - `python -m pytest -q` -> `100 passed`

3) 真实样本回归
- 真实样本路径：
  - `E:\xwechat_files\wxid_9ky6h8wz58b912_a1e5\msg\file\2026-04\samplereverse.exe`
- 新 run 目录：
  - `solve_reports\harness_runs\samplereverse_exact1_pair_quality_20260422`
- 关键 artifact：
  - `solve_reports\harness_runs\samplereverse_exact1_pair_quality_20260422\reports\tool_artifacts\samplereverse\samplereverse_compare_aware_frontier_summary.json`
  - `solve_reports\harness_runs\samplereverse_exact1_pair_quality_20260422\reports\tool_artifacts\samplereverse\samplereverse_compare_aware_strata_summary.json`
  - `solve_reports\harness_runs\samplereverse_exact1_pair_quality_20260422\reports\tool_artifacts\samplereverse\frontier_guided_1_5a3e7f46ddd474d0\samplereverse_compare_aware_guided_pool_result.json`
- 结果：
  - exact1 lane 继续真实成为 active lane：
    - `frontier_active_lane = frontier_exact1`
  - 当前仍未突破：
    - `frontier_converged_reason = distance_not_improved`
    - `frontier_stall_stage = pair_pool`
    - `improved_pair_frontier_pool_count = 0`
    - `improved_triad_frontier_pool_count = 0`
  - 但 pair 生成已经明显收缩并具备可诊断性：
    - `pair_generation_mode = exact1_locked_pairs`
    - `locked_pair_positions = [[0,1],[0,2],[0,3]]`
    - `pair_frontier_pool_size = 1`
    - `triad_frontier_pool_size = 5`
    - `pair_drop_reasons = {"exact_regressed_without_distance_escape": 15}`
  - 当前 best exact1 frontier 仍为：
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`
  - 当前 best exact2 仍为：
    - `78d540b49c590770...` -> `runtime_ci_exact_wchars=2`, `runtime_ci_distance5=246`
- 备注：
  - 本轮 harness 收尾依旧在模型阶段踩到 `gbk` 编码错误，但 compare-aware 主线 artifact 已完整产出；结论仍以 artifact 为准

4) 结论
- 本轮计划已完成，并且把瓶颈进一步定位到了更细一级：
  - 不是 exact1 lane 没锁对 pair
  - 也不是 pair gate 完全不工作
  - 而是当前 exact1 locked pairs 生成出的候选，几乎全部落入
    - `exact_regressed_without_distance_escape`
- 下一轮若继续推进，优先级应改为：
  - 重做 exact1 lane 的 pair value ranking / escape 条件
  - 或引入更强的 exact1 pair-level runtime/offline 结构信号

[2026-04-22 16:02] Exact1 Pair Escape 双池与真实回归

1) 实现完成
- 已在 `reverse_agent/strategies/compare_aware_search.py` 为 exact1 lane 接入 pair preserve/escape 双池：
  - `pair_escape_mode = exact1_dual_lane`
  - 新增 exact1 pair preserve pool / escape pool 值源
  - exact1 `1 -> 0` pair 候选不再使用旧的单一硬阈值，而改为：
    - `ci_distance5` 严格下降；或
    - `ci_distance5` 持平且 `raw_distance10` 严格下降，并且 pair-level 结构信号更优
- 新增 pair-level 结构字段并接入 exact1 frontier 排序：
  - `pair_raw_prefix_hex_16`
  - `pair_wide_ascii_contiguous_8`
  - `pair_wide_zero_high_pairs_8`
  - `pair_flaglike_tail_pairs_8`
- guided pool artifact 现新增 exact1 pair 诊断字段：
  - `pair_escape_mode`
  - `pair_preserve_pool`
  - `pair_escape_pool`
  - `pair_escape_candidates_kept`
  - `pair_escape_candidates_dropped`
  - `pair_best_preserve_candidate`
  - `pair_best_escape_candidate`
- SMT 侧已继承新的 exact1 feedback value pool 上下文，但不改外部接口

2) 自动化回归
- 新增/更新测试覆盖：
  - exact1 pair entries 返回 preserve/escape 双池元数据
  - exact1 escape signal 会过滤“降 exact 但无有效逃逸”的 pair
  - exact1 escape candidate ranked out 会被单独诊断
- 当前测试基线：
  - `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `29 passed`
  - `python -m pytest -q` -> `101 passed`

3) 真实样本回归
- 真实样本路径：
  - `E:\xwechat_files\wxid_9ky6h8wz58b912_a1e5\msg\file\2026-04\samplereverse.exe`
- 新 run 目录：
  - `solve_reports\harness_runs\samplereverse_exact1_pair_escape_20260422`
- 关键 artifact：
  - `solve_reports\harness_runs\samplereverse_exact1_pair_escape_20260422\reports\tool_artifacts\samplereverse\samplereverse_compare_aware_frontier_summary.json`
  - `solve_reports\harness_runs\samplereverse_exact1_pair_escape_20260422\reports\tool_artifacts\samplereverse\samplereverse_compare_aware_strata_summary.json`
  - `solve_reports\harness_runs\samplereverse_exact1_pair_escape_20260422\reports\tool_artifacts\samplereverse\frontier_guided_1_5a3e7f46ddd474d0\samplereverse_compare_aware_guided_pool_result.json`
- 结果：
  - exact1 lane 仍是 active lane：
    - `frontier_active_lane = frontier_exact1`
  - 当前仍未突破：
    - `frontier_converged_reason = distance_not_improved`
    - `frontier_stall_stage = pair_pool`
    - `improved_pair_frontier_pool_count = 0`
    - `improved_triad_frontier_pool_count = 0`
  - exact1 pair 诊断现在更细了：
    - `pair_escape_mode = exact1_dual_lane`
    - `pair_drop_reasons = {"exact_regressed_without_escape_signal": 15}`
    - `pair_escape_candidates_kept = 0`
    - `pair_escape_candidates_dropped = 15`
  - 当前 best exact1 frontier 仍为：
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`
  - 当前 best exact2 仍为：
    - `78d540b49c590770...` -> `runtime_ci_exact_wchars=2`, `runtime_ci_distance5=246`
- 备注：
  - 本轮 harness 仍在模型阶段踩到 `gbk` 编码错误，但 compare-aware 主线 artifact 已完整产出；结论继续以 artifact 为准

4) 结论
- 本轮计划已完成。
- 当前瓶颈进一步缩小为：
  - exact1 lane 不是没有 escape 值池
  - 而是当前 escape 候选几乎全部被新的 escape signal 判定为“仍然不够像有效逃逸”
- 下一轮若继续推进，优先级应改为：
  - 重做 exact1 escape pool 的值源本身，而不是继续调 beam 或 refine
  - 或把 runtime/offline 结构信号更直接地接进 exact1 escape signal

[2026-04-22 20:08] Exact1 Lineage Escape 值源接线与真实回归

1) 实现完成
- 已在 `reverse_agent/strategies/compare_aware_search.py` 将 exact1 escape 值源改为 lineage-driven：
  - 新增 exact1 lineage value mining
  - 逃逸值源优先吸收：
    - `source_anchor_diff`
    - exact1 lineage context entries
    - recent compare-aware payload 中的 exact1 候选
  - 不再以大半径 generic perturbation 作为主 escape source
- guided artifact 新增/补齐：
  - `pair_escape_pool_strategy`
  - `pair_escape_source_values`
  - `pair_escape_source_counts`
  - `pair_escape_source_origins`
  - `lineage_value_mining_summary`
  - `pair_escape_source_statuses`
- SMT 的 feedback/value pool 也已优先吸收 exact1 lineage-derived bytes

2) 自动化回归
- 新增/更新测试覆盖：
  - exact1 lineage source 优先于 generic perturbation
  - exact1 pair-local source 不共享 fallback 值
  - exact1 lineage source 不吸收 exact0 lane 污染
  - SMT feedback pool 会包含 exact1 lineage-derived bytes
- 当前测试基线：
  - `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `31 passed`
  - `python -m pytest -q` -> `103 passed`

3) 真实样本回归
- 新 run 目录：
  - `solve_reports\harness_runs\samplereverse_exact1_lineage_escape_20260422`
- 关键 artifact：
  - `solve_reports\harness_runs\samplereverse_exact1_lineage_escape_20260422\reports\tool_artifacts\samplereverse\samplereverse_compare_aware_frontier_summary.json`
  - `solve_reports\harness_runs\samplereverse_exact1_lineage_escape_20260422\reports\tool_artifacts\samplereverse\samplereverse_compare_aware_strata_summary.json`
  - `solve_reports\harness_runs\samplereverse_exact1_lineage_escape_20260422\reports\tool_artifacts\samplereverse\frontier_guided_1_5a3e7f46ddd474d0\samplereverse_compare_aware_guided_pool_result.json`
- 结果：
  - exact1 lane 仍是 active lane：
    - `frontier_active_lane = frontier_exact1`
  - 当前 best frontier 仍未突破：
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`
  - lineage-driven source 已真实生效：
    - `pair_escape_pool_strategy = lineage_driven_exact1`
    - `pair_escape_source_values` 不再是纯 generic perturbation
    - 例如位置 `0` 已出现 `120/164/83/80`，位置 `1` 已出现 `213/222/116/11`
  - 但本轮仍未出现 escape kept：
    - `pair_escape_candidates_kept = 0`
    - `pair_escape_candidates_dropped = 15`
    - `improved_pair_frontier_pool_count = 0`
  - 新瓶颈已更清楚：
    - `pair_escape_source_values` 已非空
    - 但 `pair_escape_source_statuses` 仍显示 `source_empty`
    - 这说明问题已从“没有 escape source”收缩为“escape source 没有进入 pair_profiles 前排”，即 top-per-pair 截断/排序层面仍在压制 escape 候选
- 备注：
  - harness 收尾依旧在模型阶段踩到 `gbk` 编码错误，但 compare-aware artifact 完整；结论继续以 artifact 为准

4) 结论
- 本轮计划已完成，并且把 exact1 瓶颈再缩小了一层：
  - 不是没有 lineage-driven source
  - 而是这些 source 生成的 escape 候选还没成功穿过 pair profile 的前排截断
- 下一轮若继续推进，优先级应改为：
  - 重做 exact1 pair profile 的保留/截断策略，使 escape 候选能进入 `pair_profiles[:top_per_pair]`
  - 或给 exact1 escape 候选单独的 rank lane，避免在 pair profile 阶段就被 preserve 候选完全淹没

## 2026-04-22 21:33:00 Exact1 Pair Profile 双通道保留已落地

1) 本轮目标
- 按最新计划把 exact1 lane 的 pair profile 从单通道 `top_per_pair` 截断改成 preserve/escape 双通道保留。
- 目标不是直接解题，而是验证 lineage-driven escape 候选能否真正进入 `pair_profiles`，并把瓶颈从 `profile truncation` 继续往后推。

2) 代码改动
- 修改文件：
  - `reverse_agent/strategies/compare_aware_search.py`
  - `tests/test_compare_aware_search_strategy.py`
- 关键实现：
  - 新增 exact1 profile 配额：
    - `EXACT1_PAIR_PROFILE_PRESERVE_TOP = 4`
    - `EXACT1_PAIR_PROFILE_ESCAPE_TOP = 2`
  - 新增 `_exact1_escape_profile_sort_key()`，让 escape 候选先在自己的 lane 内排序。
  - `_top_compare_aware_pair_entries()` 现在会：
    - 分别生成 `pair_profile_preserve_entries` / `pair_profile_escape_entries`
    - 分别保留 `pair_profile_kept_preserve` / `pair_profile_kept_escape`
    - 产出 `pair_profile_drop_reasons` / `pair_profile_truncation_summary`
  - `_diverse_pair_frontier_pool()` 现在会消费 profile 级诊断，并把 exact1 escape 状态区分为：
    - `profile_source_empty`
    - `profile_ranked_out`
    - `frontier_gate_failed`
    - `source_used`
  - `run_compare_aware_guided_pool()` payload 和返回值已带出新的 profile 级字段。
  - `run_compare_aware_smt()` / `_smt_feedback_value_pools()` 已改为优先吸收 exact1 `pair_profile_kept_escape`，其次 `pair_profile_kept_preserve`，再回退到 lineage source。

3) 自动化回归
- 测试结果：
  - `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `33 passed`
  - `python -m pytest -q` -> `105 passed`
- 新增覆盖点：
  - exact1 pair profile 会保留 escape lane
  - profile 层失败会被归因到 `profile_ranked_out`
  - SMT feedback pool 会继承 exact1 kept profile values

4) 真实样本回归
- 新 run 目录：
  - `solve_reports\harness_runs\samplereverse_exact1_profile_escape_20260422`
- 关键 artifact：
  - `solve_reports\harness_runs\samplereverse_exact1_profile_escape_20260422\reports\tool_artifacts\samplereverse\samplereverse_compare_aware_frontier_summary.json`
  - `solve_reports\harness_runs\samplereverse_exact1_profile_escape_20260422\reports\tool_artifacts\samplereverse\samplereverse_compare_aware_strata_summary.json`
  - `solve_reports\harness_runs\samplereverse_exact1_profile_escape_20260422\reports\tool_artifacts\samplereverse\frontier_guided_1_5a3e7f46ddd474d0\samplereverse_compare_aware_guided_pool_result.json`
- 真实结果：
  - exact1 lane 仍然是 active lane：
    - `frontier_active_lane = frontier_exact1`
  - 当前 best frontier 仍未突破：
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`
  - exact1 escape 候选已经真正进入 pair profile：
    - `pair_profile_kept_escape = 6`
    - 每个 locked pair 都从原来的“进不了 profile”变成了：
      - `preserve_kept = 4`
      - `escape_kept = 2`
  - profile 层已不再是主瓶颈：
    - `pair_escape_source_statuses = frontier_gate_failed`
    - 不再是上一轮的 `source_empty`
  - 但 frontier gate 仍未放行任何 exact1 escape 候选：
    - `pair_escape_candidates_kept = 0`
    - `pair_escape_candidates_dropped = 15`
    - `improved_pair_frontier_pool_count = 0`
  - frontier 仍以：
    - `frontier_converged_reason = distance_not_improved`
    - `frontier_stall_stage = pair_pool`
    收敛

5) 结论
- 本轮计划已完成，并且把瓶颈再次后移了一层：
  - 旧瓶颈：escape 候选进不了 pair profile
  - 新瓶颈：escape 候选已经进入 profile，但全部死在 exact1 frontier gate
- 这说明下一轮不该再改值源，也不该再改 profile 截断；应转向：
  - 重做 exact1 escape signal / gate 排序
  - 或给 exact1 escape lane 一套更能识别“值得牺牲 exact1 换 distance 改善”的保留规则

## 2026-04-22 22:16:00 Exact1 Escape Signal 已结构化并完成真实样本回归

1) 本轮目标
- 不再改 exact1 的值源和 pair profile 保留，转而重做 exact1 escape signal / frontier gate。
- 目标是把 exact1 escape 的失败从“单一布尔 gate”细化成可诊断的 `local_escape / hard_escape` 两类，并验证是否有 escape 能穿过 gate。

2) 代码改动
- 修改文件：
  - `reverse_agent/strategies/compare_aware_search.py`
  - `tests/test_compare_aware_search_strategy.py`
- 关键实现：
  - `_exact1_pair_escape_signal()` 从布尔返回改成结构化结果，输出：
    - `passed`
    - `score`
    - `reason`
    - `lane`
    - `distance_delta`
    - `raw_delta`
    - `locality_score`
  - 新增 exact1 gate 上限：
    - `EXACT1_PAIR_ESCAPE_KEEP_SCORE_MAX = 3`
  - exact1 escape 现在会在 gate 前被分成：
    - `local_escape`
    - `hard_escape`
  - `_diverse_pair_frontier_pool()` 对 exact1 现在会：
    - 标注 `pair_escape_signal_score`
    - 标注 `pair_escape_signal_reason`
    - 标注 `pair_escape_lane`
    - 按 `gate_filtered_hard_escape` / `gate_filtered_local_escape` 分流
  - guided artifact 新增：
    - `pair_gate_input_summary`
    - `pair_gate_failed_escape`
    - `pair_gate_kept_escape`
  - `pair_escape_source_statuses` 在 exact1 lane 现在固定使用新状态：
    - `profile_source_empty`
    - `profile_ranked_out`
    - `gate_filtered_hard_escape`
    - `gate_filtered_local_escape`
    - `gate_kept_escape`
  - SMT feedback 值优先级已改成：
    - exact1 preserve kept
    - exact1 gate kept escape
    - 再回退 lineage values

3) 自动化回归
- 测试结果：
  - `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `34 passed`
  - `python -m pytest -q` -> `106 passed`
- 新增/更新覆盖点：
  - exact1 escape signal 会区分 `local_escape` 与 `hard_escape`
  - exact1 gate 不再依赖单个布尔返回
  - SMT feedback pool 会优先吸收 `pair_gate_kept_escape`

4) 真实样本回归
- 新 run 目录：
  - `solve_reports\harness_runs\samplereverse_exact1_escape_signal_20260422`
- 关键 artifact：
  - `solve_reports\harness_runs\samplereverse_exact1_escape_signal_20260422\reports\tool_artifacts\samplereverse\frontier_guided_1_5a3e7f46ddd474d0\samplereverse_compare_aware_guided_pool_result.json`
  - `solve_reports\harness_runs\samplereverse_exact1_escape_signal_20260422\reports\tool_artifacts\samplereverse\samplereverse_compare_aware_frontier_summary.json`
  - `solve_reports\harness_runs\samplereverse_exact1_escape_signal_20260422\reports\tool_artifacts\samplereverse\samplereverse_compare_aware_strata_summary.json`
- 真实结果：
  - exact1 lane 仍然是 active lane：
    - `frontier_active_lane = frontier_exact1`
  - best frontier 仍未突破：
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`
  - gate 细分已经真实生效：
    - `pair_gate_kept_escape = 0`
    - `pair_gate_failed_escape = 15`
    - `pair_drop_reasons = { gate_filtered_hard_escape: 7, gate_filtered_local_escape: 8 }`
  - 当前 exact1 escape 候选被清楚分成两类：
    - `hard_escape_far_from_anchor`
    - `local_escape_but_signal_weak`
  - `pair_escape_source_statuses` 已不再是旧的一锅端失败：
    - `0,3 -> gate_filtered_hard_escape`
    - `0,1 -> gate_filtered_hard_escape`
    - `0,2 -> gate_filtered_hard_escape`
  - `pair_gate_input_summary` 已能逐 pair 显示：
    - 哪些候选是 `hard_escape`
    - 哪些候选是 `local_escape`
    - 以及对应的 `pair_escape_signal_score`
  - 但本轮仍未产生任何 gate-kept escape，也没有新的 improved pair：
    - `pair_escape_candidates_kept = 0`
    - `improved_pair_frontier_pool_count = 0`
    - `frontier_stall_stage = pair_pool`
    - `frontier_converged_reason = distance_not_improved`

5) 结论
- 本轮计划已完成，并且把瓶颈再次往前推进了一步：
  - 旧瓶颈：escape 进不了 profile
  - 上一轮瓶颈：escape 进了 profile，但全被同一个布尔 gate 拒掉
  - 当前瓶颈：exact1 gate 已能识别 `hard_escape` 和 `local_escape`，但 `local_escape` 仍然全部是 `signal_weak`
- 这说明下一轮不该再打 source/profile，也不该再扩 beam；最值得继续做的是：
  - 重做 `local_escape` 的 signal 分数与阈值
  - 或调整 exact1 locked pair 集合，让真正接近盆地的 local escape 能出现 `gate_kept_escape`

## 2026-04-23 继续推进：exact1 local escape 评分 + pair-set 轮换

1) 实现改动
- 已完成 exact1 `local_escape` / `hard_escape` 的新一轮策略升级：
  - `_exact1_pair_escape_signal()` 现在会输出更细的局部移动特征：
    - `local_step_count`
    - `large_step_count`
    - `local_move_score`
  - exact1 `local_escape` 新增了更宽的“弱可接受区间”：
    - `local_escape_within_tight_tolerance`
    - `local_escape_near_anchor`
    - `local_escape_weak_but_acceptable`
  - exact1 gate 上限更新为：
    - `EXACT1_PAIR_ESCAPE_KEEP_SCORE_MAX = 5`
- `_diverse_pair_frontier_pool()` 的 exact1 分支已改成：
  - 先按 pair 内部挑 `top_local_escape_per_pair`
  - 再收 preserve
  - hard escape 只保留极少量诊断样本
- exact1 per-pair 诊断已补齐：
  - `pair_escape_lane_counts`
  - `pair_escape_status_by_lane`
  - `pair_best_local_escape`
  - `pair_best_hard_escape`
- exact1 guided 现在会评估两套 pair 集：
  - `primary_pair_set`
  - `alternate_pair_set`
  - 并把比较结果写入：
    - `pair_set_mode`
    - `alternate_locked_pair_positions`
    - `pair_set_comparison_summary`
- SMT exact1 值优先级已同步更新：
  - exact1 preserve kept
  - exact1 `pair_gate_kept_escape`
  - exact1 `pair_best_local_escape`
  - 再回退 lineage values

2) 自动化回归
- 测试结果：
  - `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `36 passed`
  - `python -m pytest -q` -> `108 passed`
- 新增覆盖点：
  - exact1 per-pair 能同时区分 `local_escape` 与 `hard_escape`
  - exact1 候补 pair 集会被实际评估
  - exact1 local escape 进入 gate 后的诊断字段会原样落盘

3) 真实样本回归
- 新 run 目录：
  - `solve_reports\\harness_runs\\samplereverse_exact1_local_escape_pairs_20260423`
- 关键 artifact：
  - `solve_reports\\harness_runs\\samplereverse_exact1_local_escape_pairs_20260423\\reports\\tool_artifacts\\samplereverse\\frontier_guided_1_5a3e7f46ddd474d0\\samplereverse_compare_aware_guided_pool_result.json`
  - `solve_reports\\harness_runs\\samplereverse_exact1_local_escape_pairs_20260423\\reports\\tool_artifacts\\samplereverse\\samplereverse_compare_aware_frontier_summary.json`
  - `solve_reports\\harness_runs\\samplereverse_exact1_local_escape_pairs_20260423\\reports\\tool_artifacts\\samplereverse\\samplereverse_compare_aware_strata_summary.json`
- 真实结果：
  - exact1 仍是 active lane：
    - `frontier_active_lane = frontier_exact1`
  - best frontier 仍未突破：
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`
  - pair-set 轮换已真实执行：
    - `primary_pair_set = [[0,1],[0,2],[0,3]]`
    - `alternate_pair_set = [[1,2],[1,3],[2,3]]`
    - `exact1_pair_set_winner = primary_pair_set`
  - 当前两套 pair 集结果一致，都没有放行 exact1 local escape：
    - `pair_gate_kept_escape = 0`
    - `exact1_local_escape_kept_count = 0`
    - `exact1_hard_escape_filtered_count = 6`
  - 这轮新的关键发现不是“local 还太弱”，而是：
    - 当前 exact1 escape 候选已经全部被判成 `hard_escape`
    - `pair_escape_lane_counts` 显示：
      - `0,3 -> { local_escape: 0, hard_escape: 2 }`
      - `0,1 -> { local_escape: 0, hard_escape: 2 }`
      - `0,2 -> { local_escape: 0, hard_escape: 2 }`
    - `pair_best_local_escape = {}`
    - `pair_best_hard_escape` 非空
  - 也就是说，这轮实现成功验证了：
    - pair-set 轮换本身不是当前瓶颈
    - profile 和 source 也不是当前瓶颈
    - 当前真正卡住的是 exact1 候选在进入 gate 前就已经“偏离到只剩 hard_escape”

4) 结论
- 本轮计划已完成，但没有出现新的 `gate_kept_escape`，也没有 runtime 改善。
- 瓶颈已经进一步收缩为：
  - exact1 当前 locked pair / 值源组合产生的 escape 候选，已经不再形成任何 `local_escape`
  - 所以下一轮不该继续只调 gate 阈值，而应优先检查：
    - exact1 locked pair 集是否仍然选错
    - exact1 preserve/escape 的局部生成是否过早把候选推离 exact1 盆地

[2026-04-23 13:18 China Standard Time] Exact1 local-neighbor candidate generation landed

1) 代码与测试
- 已在 `reverse_agent/strategies/compare_aware_search.py` 实现 exact1 的局部邻域候选生成：
  - exact1 pair 生成不再默认混入远距离 fallback 值；
  - 现在会为每个 locked pair 生成 `preserve_neighbors` 与 `escape_neighbors`；
  - 每个候选都会附带：
    - `pair_candidate_origin`
    - `pair_mutation_radius`
    - `pair_neighbor_mode`
    - `pair_value_origin_by_pos`
- guided payload / diagnostics 新增：
  - `pair_neighbor_generation_summary`
  - `pair_local_escape_candidate_count`
  - `pair_hard_escape_candidate_count`
  - `pair_mutation_radius_summary`
- 测试已更新：
  - `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `36 passed`
  - `python -m pytest -q` -> `108 passed`

2) 真实样本回归
- 新 run:
  - `solve_reports\\harness_runs\\samplereverse_exact1_local_neighbor_candidates_20260423`
- 目标：
  - `E:\\xwechat_files\\wxid_9ky6h8wz58b912_a1e5\\msg\\file\\2026-04\\samplereverse.exe`
- 注意：
  - harness 在最后模型兜底阶段因 `gbk` 编码错误报错退出；
  - 但 compare-aware strategy 的 artifact 已完整产出，本轮结论可用。

3) 关键结果
- exact1 候选生成方向第一次出现了实质性突破：
  - `pair_local_escape_candidate_count = 6`
  - `pair_hard_escape_candidate_count = 0`
  - `pair_escape_lane_counts`：
    - `0,2 -> { local_escape: 2, hard_escape: 0 }`
    - `0,1 -> { local_escape: 2, hard_escape: 0 }`
    - `0,3 -> { local_escape: 2, hard_escape: 0 }`
- 说明这一轮已经把瓶颈从“生成出来全是 hard_escape”推进到了：
  - `local_escape` 已经生成出来了；
  - 但仍全部停在 `gate_filtered_local_escape`；
  - `pair_gate_kept_escape = 0`
  - `improved_pair_frontier_pool_count = 0`
- 当前 best runtime 仍未变：
  - exact2:
    - `78d540b49c590770...` -> `runtime_ci_exact_wchars=2`, `runtime_ci_distance5=246`
  - exact1:
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`
- pair-set 轮换结果：
  - `exact1_pair_set_winner = primary_pair_set`
  - `alternate_pair_set` 也真实执行了，但没有优于主集合

4) 结论
- 本轮计划已完成，而且是有效推进：
  - 当前主问题已经不再是 source/profile/pair-set/candidate-generation；
  - 现在真正卡住的是 exact1 `local_escape` 的 gate / refine handoff。
- 下一轮优化重点应切换为：
  - 为什么 `local_escape_but_signal_weak` 仍无法穿过 exact1 gate；
  - 哪些 `local_escape` 候选最接近可放行边界；
  - 是否要针对 exact1 `local_escape` 改 gate 分段或 rerank，而不是继续重做候选生成。

## 2026-04-23 - Exact1 borderline local_escape handoff implemented and regressed

1) 代码变更
- 在 `CompareAwareSearchStrategy` 的 exact1 pair gate 中新增三段状态：
  - `keep`
  - `borderline`
  - `reject`
- `local_escape` 的弱可接受候选现在会进入 `pair_frontier_pool` 的 borderline handoff，而不是继续全部落入 `gate_filtered_local_escape`。
- 新增并贯通 artifact 字段：
  - `pair_borderline_escape_candidates`
  - `pair_local_escape_borderline_count`
  - `pair_local_escape_reject_count`
  - `pair_gate_borderline_summary`
- pair-set 选择、guided payload、return payload、SMT feedback value pool、frontier/strata summary 都已接入 borderline 信息。
- stall 归因更新：
  - 如果 exact1 已产生 borderline / kept escape，但 runtime 未改善，不再归因到 `pair_pool`；
  - 改为进入 `frontier_refine` 停滞，表示 handoff 已发生但后续没有接住。

2) 自动化测试
- `python -m pytest -q tests/test_compare_aware_search_strategy.py`
  - `37 passed`
- `python -m pytest -q`
  - `109 passed`

3) 真实样本回归
- 新 run:
  - `solve_reports\\harness_runs\\samplereverse_exact1_borderline_escape_20260423`
- 目标：
  - `E:\\xwechat_files\\wxid_9ky6h8wz58b912_a1e5\\msg\\file\\2026-04\\samplereverse.exe`
- 注意：
  - harness 在最后模型兜底阶段仍因 `gbk` 编码错误退出；
  - compare-aware artifact 已完整生成，本轮结论可用。

4) 关键结果
- exact1 frontier guided:
  - `pair_local_escape_candidate_count = 6`
  - `pair_local_escape_borderline_count = 6`
  - `pair_local_escape_reject_count = 0`
  - `pair_hard_escape_candidate_count = 0`
  - `pair_gate_kept_escape = 0`
  - `pair_borderline_escape_candidates = 3`
  - `improved_pair_frontier_pool_count = 0`
- 三个 locked pair 都出现 borderline：
  - `0,2 -> gate_borderline_escape`
  - `0,1 -> gate_borderline_escape`
  - `0,3 -> gate_borderline_escape`
- 进入 pair frontier 的 borderline 候选：
  - `563e7b46ddd474d0`, pair `[0,2]`, `ci_distance5=558`, `raw_distance10=558`
  - `56427f46ddd474d0`, pair `[0,1]`, `ci_distance5=609`, `raw_distance10=609`
  - `563e7f4addd474d0`, pair `[0,3]`, `ci_distance5=725`, `raw_distance10=757`
- 当前 runtime best 未改善：
  - exact2:
    - `78d540b49c590770...` -> `runtime_ci_exact_wchars=2`, `runtime_ci_distance5=246`
  - exact1:
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`
- summary 归因已更新：
  - `frontier_converged_reason = distance_not_improved`
  - `frontier_stall_stage = frontier_refine`
  - `exact1_pair_set_winner = primary_pair_set`
  - `exact1_local_escape_borderline_count = 6`
  - `exact1_hard_escape_filtered_count = 0`

5) 结论
- 本轮计划完成，并把瓶颈从“local_escape 无法穿过 gate”推进为：
  - local_escape 已经可以作为 borderline 进入 pair frontier；
  - 但 borderline 质量太差，runtime compare 后没有改善；
  - refine/SMT 没有从这些 borderline 中接出更优 exact1。
- 下一轮不应继续放宽 gate；应重做 exact1 borderline 的质量筛选 / rerank：
  - 优先约束 `ci_distance5/raw_distance10` 的最大退让；
  - 将 `pair_mutation_radius=4` 的候选拆出单独诊断；
  - 重点寻找距离不爆炸的 local escape，而不是让所有 weak local escape 都进入 handoff。

## 2026-04-23 - Exact1 near/wide local_escape quality band implemented and regressed

1) 代码变更
- 在 exact1 pair escape 信号中把 borderline local escape 分成：
  - `near_local_escape`
  - `wide_local_escape`
- 只有 `near_local_escape` 进入主 `pair_frontier_pool` / refine / SMT handoff。
- `wide_local_escape` 只保留诊断，不再占用主 handoff 配额。
- 新增 exact1 single-byte guard：
  - pair escape 组合前先评估单字节候选；
  - 距离/结构过差的单字节值不进入 pair escape 组合。
- 新增/贯通诊断字段：
  - `pair_escape_quality_band`
  - `pair_near_local_escape_candidates`
  - `pair_wide_local_escape_candidates`
  - `pair_single_byte_guard_summary`
  - `pair_radius_band_counts`
  - `pair_borderline_quality_reason`
  - `exact1_near_local_escape_count`
  - `exact1_wide_local_escape_count`
  - `exact1_best_near_local_distance`
  - `exact1_pair_quality_winner`
- SMT value pool 改为只吸收 exact1 `near_local_escape`，不再吸收 wide-local diagnostic candidates。

2) 自动化测试
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "borderline or local_escape or smt or pair_set or pair_entries"`
  - `12 passed`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py`
  - `39 passed`
- `python -m pytest -q`
  - `111 passed`

3) 真实样本回归
- 新 run:
  - `solve_reports\\harness_runs\\samplereverse_exact1_near_wide_local_escape_20260423`
- 目标：
  - `E:\\xwechat_files\\wxid_9ky6h8wz58b912_a1e5\\msg\\file\\2026-04\\samplereverse.exe`
- 注意：
  - harness 在最后模型兜底阶段仍因 `gbk` 编码错误退出；
  - compare-aware artifact 已完整生成，本轮结论可用。

4) 关键结果
- exact1 frontier guided:
  - `pair_local_escape_candidate_count = 3`
  - `pair_local_escape_borderline_count = 0`
  - `pair_near_local_escape_count = 0`
  - `pair_wide_local_escape_count = 0`
  - `pair_gate_kept_escape = 1`
  - `improved_pair_frontier_pool_count = 0`
- single-byte guard 过于强势：
  - 三个主 locked pair 的左右位置都出现类似：
    - `input = 4`
    - `kept = 1`
    - `dropped = 4`
  - 实际 escape profile 只剩 exact1 base 自身：
    - `5a3e7f46ddd474d0`
- 当前 runtime best 未改善：
  - exact2:
    - `78d540b49c590770...` -> `runtime_ci_exact_wchars=2`, `runtime_ci_distance5=246`
  - exact1:
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`
- summary：
  - `frontier_converged_reason = distance_not_improved`
  - `frontier_stall_stage = frontier_refine`
  - `exact1_near_local_escape_count = 0`
  - `exact1_wide_local_escape_count = 0`
  - `exact1_best_near_local_distance = null`

5) 结论
- 本轮成功阻止了上一轮 `558+` 的 wide weak borderline 进入主 handoff。
- 但 guard 现在太硬，导致没有任何真实 near/wide local escape 进入分层，只剩 base fallback。
- 下一轮不应继续改 gate；应把 single-byte guard 从硬过滤改成 soft rank / diagnostic-first：
  - 保留少量 guard-rejected 但 radius 小的候选进入 `wide_local_escape` 诊断；
  - 允许每个位置至少 1 个非 base local candidate 进入 pair 组合；
  - 用 pair 阶段质量筛选再决定 near/wide，而不是在单字节阶段提前杀光。

## 2026-04-23 - Exact1 single-byte soft guard implemented and regressed

1) 代码变更
- 将 exact1 single-byte guard 从硬过滤改成三态：
  - `guard_passed`
  - `guard_soft_rejected`
  - `guard_hard_rejected`
- 每个 exact1 escape 位置现在固定保留：
  - base byte
  - 全部 `guard_passed`
  - 最多 1 个低 radius 的 `guard_soft_rejected`
- 新增并贯通诊断字段：
  - `pair_single_byte_guard_candidates`
  - `pair_single_byte_guard_status_counts`
  - `pair_guard_soft_promoted_values`
  - `pair_guard_nonbase_starved`
- pair 阶段仍负责最终 near/wide/keep 判定；
  - SMT 继续只吸收 `pair_gate_kept_escape` / `near_local_escape`，不吸收 `wide_local_escape` 或纯 guard diagnostics。

2) 自动化测试
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "soft_guard or borderline or local_escape or smt or pair_set or pair_entries"`
  - `13 passed`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py`
  - `40 passed`
- `python -m pytest -q`
  - `112 passed`

3) 真实样本回归
- 新 run:
  - `solve_reports\\harness_runs\\samplereverse_exact1_soft_guard_20260423`
- 目标：
  - `E:\\xwechat_files\\wxid_9ky6h8wz58b912_a1e5\\msg\\file\\2026-04\\samplereverse.exe`
- 注意：
  - harness 末尾仍因 `gbk` 编码错误退出；
  - compare-aware artifacts 已完整生成，本轮结论有效。

4) 关键结果
- soft guard 已经恢复 non-base 值进入 exact1 pair 组合：
  - `pair_guard_soft_promoted_values` 非空：
    - `0,1 -> {0: [87], 1: [59]}`
    - `0,2 -> {0: [87], 2: [124]}`
    - `0,3 -> {0: [87], 3: [67]}`
  - `pair_guard_nonbase_starved` 全部为 `false`
  - 三组主 pair 的单字节 guard 统计都变成：
    - `guard_passed = 0`
    - `guard_soft_rejected = 4`
    - `guard_hard_rejected = 0`
- exact1 frontier guided:
  - `pair_local_escape_candidate_count = 6`
  - `pair_gate_kept_escape = 1`
  - `pair_near_local_escape_count = 0`
  - `pair_wide_local_escape_count = 3`
  - `improved_pair_frontier_pool_count = 0`
- 新 non-base 组合已经进入 pair gate，但质量仍差：
  - `5a3b7f46ddd474d0` -> `ci_distance5 = 870`
  - `5a3e7c46ddd474d0` -> `ci_distance5 = 751`
  - `5a3e7f43ddd474d0` -> `ci_distance5 = 708`
  - 全部被归为 `wide_local_escape`
- 当前 runtime best 仍未改善：
  - exact2:
    - `78d540b49c590770...` -> `runtime_ci_exact_wchars=2`, `runtime_ci_distance5=246`
  - exact1:
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`
- summary：
  - `frontier_converged_reason = distance_not_improved`
  - `frontier_stall_stage = frontier_refine`
  - `best_frontier_after` 仍是 `5a3e7f46ddd474d0...`

5) 结论
- 本轮成功证明：问题已经不再是 exact1 guard 把非 base 值提前杀光。
- 新瓶颈是 soft-promoted 值的质量：
  - 值能够进入 pair 组合；
  - 但一离开 exact1 anchor 就迅速掉进 `wide_local_escape`，没有任何 near-local 改善。
- 下一轮默认方向不应继续放宽 gate；
  - 应重排 exact1 soft-promotion 排序，让被提升的单字节值更贴近 exact1 局部结构，而不是只按小 radius 兜底；
  - 优先把 `guard_soft_rejected` 再细分成“local-compatible”与“distance-explosive”两类；
  - 目标是先把至少 1 个 non-base 候选推进到 `near_local_escape`，再看 refine/SMT 是否能接住。

## 2026-04-23 - Exact1 soft-promotion quality rerank implemented and regressed

1) 代码变更
- 将 exact1 `guard_soft_rejected` 再细分为：
  - `local_compatible_soft`
  - `distance_explosive_soft`
- exact1 soft-promotion 排序不再只按 `radius`：
  - 先比较单字节 `ci_distance5` 退让
  - 再比较单字节 `raw_distance10` 退让
  - 再比较单字节结构退让
  - 再比较 `radius`
  - 最后比较来源优先级
- 新增并贯通诊断字段：
  - `pair_guard_soft_quality_band`
  - `pair_guard_soft_rank_summary`
  - `pair_guard_soft_distance_delta`
  - `pair_guard_soft_raw_delta`
  - `pair_guard_soft_structure_delta`

2) 自动化测试
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "soft_guard or borderline or local_escape or smt or pair_set or pair_entries"`
  - `14 passed`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py`
  - `41 passed`
- `python -m pytest -q`
  - `113 passed`

3) 真实样本回归
- 新 run:
  - `solve_reports\\harness_runs\\samplereverse_exact1_soft_quality_rerank_20260423`
- 目标：
  - `E:\\xwechat_files\\wxid_9ky6h8wz58b912_a1e5\\msg\\file\\2026-04\\samplereverse.exe`
- 注意：
  - harness 末尾仍因 `gbk` 编码错误退出；
  - compare-aware artifacts 已完整生成，本轮结论有效。

4) 关键结果
- 新增 soft-quality 诊断已完整写出，且可以确认当前 exact1 soft 值全部属于：
  - `distance_explosive_soft`
- promoted 值没有发生实质变化：
  - `0,1 -> {0: [87], 1: [59]}`
  - `0,2 -> {0: [87], 2: [124]}`
  - `0,3 -> {0: [87], 3: [67]}`
- 每个 promoted 值的单字节退让仍然过大，且结构统一劣化：
  - pos0:
    - `87 -> dist_delta 682 raw_delta 650`
    - `93 -> dist_delta 776 raw_delta 744`
  - pos2:
    - `124 -> dist_delta 493 raw_delta 461`
    - `131 -> dist_delta 542 raw_delta 536`
  - pos3:
    - `67 -> dist_delta 450 raw_delta 474`
    - `74 -> dist_delta 492 raw_delta 496`
  - 所有 soft 值的 `structure_delta` 都是：
    - `[-1, -1, 0, -1, -1, 0]`
  - 所有 soft 值来源也仍然只有：
    - `escape_neighbor`
- exact1 frontier guided 结果仍未前进：
  - `pair_near_local_escape_count = 0`
  - `pair_wide_local_escape_count = 3`
  - `pair_gate_kept_escape = 1`
  - `improved_pair_frontier_pool_count = 0`
- 当前 runtime best 仍未改善：
  - exact2:
    - `78d540b49c590770...` -> `runtime_ci_exact_wchars=2`, `runtime_ci_distance5=246`
  - exact1:
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`
- summary：
  - `frontier_converged_reason = distance_not_improved`
  - `frontier_stall_stage = frontier_refine`

5) 结论
- 本轮验证了：当前问题已经不再是 soft-promotion 排序顺序本身。
- 真正的新瓶颈是 exact1 escape source 的内容：
  - soft 值虽然能排序；
  - 但候选全集本身全部来自 `escape_neighbor`；
  - 且全部同时表现为“distance 爆炸 + structure 统一退化”。
- 下一轮默认方向不应继续调 gate 或单字节排序；
  - 应回到 exact1 escape source，优先让 `profile_near` / `incoming_near` / `lineage_near` 真正进入 soft-promotion 候选集；
  - 若真实样本里这些来源依旧为空，再考虑把 exact1 pair 位集合从 `0,*` 过度中心化里移开。

## 2026-04-24 - Exact1 source projection implemented and regressed

1) 代码变更
- 在 exact1 source pipeline 中加入“远端 raw source -> 局部 projected values”机制：
  - `profile / incoming / lineage` 的远端 byte 不再直接丢弃；
  - 先按相对 exact1 anchor 的符号方向投影到局部 `[-4,+4]` 邻域；
  - projected 值分别进入 preserve / escape source 池。
- 新增并贯通 exact1 source projection 诊断字段：
  - `pair_escape_source_projected_values`
  - `pair_escape_source_projected_origins`
  - `lineage_projection_summary`
  - `pair_escape_source_reject_reasons`
- 修复一个真实影响排序的 bug：
  - `origin_priority=0` 之前被 `or` 误判为 falsy，导致 `lineage_projected` 无法在 soft-promotion 排序中发挥优先级。

2) 自动化测试
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "soft_guard or local_escape or pair_entries or lineage_value"`
  - `11 passed`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py`
  - `43 passed`
- `python -m pytest -q`
  - `115 passed`

3) 真实样本回归
- 新 run:
  - `solve_reports\\harness_runs\\samplereverse_exact1_source_projection_20260424`
- 目标：
  - `E:\\xwechat_files\\wxid_9ky6h8wz58b912_a1e5\\msg\\file\\2026-04\\samplereverse.exe`
- 注意：
  - harness 末尾仍因 `gbk` 编码错误退出；
  - compare-aware artifacts 已完整生成，本轮结论有效。

4) 关键结果
- projected local values 已真实生成，并进入 guided artifact：
  - `0,1`:
    - pos0 -> `89,88,87,86,91,92,93,94`
    - pos1 -> `61,60,59,58,63,64,65,66`
  - `0,2`:
    - pos0 -> `89,88,87,86,91,92,93,94`
    - pos2 -> `126,125,124,123,128,129,130,131`
  - `0,3`:
    - pos0 -> `89,88,87,86,91,92,93,94`
    - pos3 -> `71,72,73,74,69,68,67,66`
- projected origins 已不再是空壳：
  - 多个 projected 值同时带有 `profile_projected` / `lineage_projected`
  - 证明远端 source 已被真正投影进 exact1 局部候选空间
- `pair_escape_source_reject_reasons` 进一步确认：
  - projected values 不只是生成了；
  - 它们确实进入了 pair pool：
    - `0,1` -> reached `87 / 59`
    - `0,2` -> reached `87 / 124`
    - `0,3` -> reached `87 / 67`
- 但 exact1 质量仍未突破：
  - `pair_guard_soft_promoted_values` 仍是：
    - `0,1 -> {0:[87],1:[59]}`
    - `0,2 -> {0:[87],2:[124]}`
    - `0,3 -> {0:[87],3:[67]}`
  - `pair_guard_soft_quality_band` 仍全部为：
    - `distance_explosive_soft`
  - `pair_near_local_escape_count = 0`
  - `pair_wide_local_escape_count = 3`
  - `improved_pair_frontier_pool_count = 0`
- runtime best 仍未改善：
  - exact2:
    - `78d540b49c590770...` -> `runtime_ci_exact_wchars=2`, `runtime_ci_distance5=246`
  - exact1:
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`
- summary：
  - `frontier_converged_reason = distance_not_improved`
  - `frontier_stall_stage = frontier_refine`

5) 结论
- 本轮成功证明：exact1 的新瓶颈已经不再是 source generation。
- 远端 lineage/profile 证据已经被转成 projected local values，并且至少有一部分真的进入了 pair pool。
- 当前更具体的瓶颈是 projected 值的质量：
  - source projection 成功；
  - soft-promotion 也接住了 projected 值；
  - 但这些值仍全部表现为 `distance_explosive_soft`，进入 pair 后仍只产出 `wide_local_escape`。
- 下一轮默认方向不应继续打 source projection；
  - 应主攻 exact1 projected 值到 `local_compatible_soft` 的质量筛选；
  - 优先压缩 projected 邻域方向与半径，让进入 soft-promotion 的 projected 值不再同时携带“方向对但距离仍爆炸”的问题；
  - 若再一轮仍只有 `wide_local_escape`，再考虑把 pair 组合规则做成“projected-only preserve lane”而不是继续扩 source。

## 2026-04-24 exact1 projected-quality regression

1) 代码改动
- 在 `reverse_agent/strategies/compare_aware_search.py` 中继续收紧 exact1 projected source：
  - 新增 `EXACT1_PROJECTED_STEP_LIMIT = 2` 与 `EXACT1_PROJECTED_KEEP_PER_DIRECTION = 1`，默认只保留 `±1/±2` 投影步长。
  - 新增 `_exact1_projected_value_quality()`，把 projected 值分成：
    - `projected_local_compatible`
    - `projected_distance_explosive`
  - exact1 projected 值现在会记录：
    - `pair_escape_source_projected_quality_band`
    - `pair_escape_source_projected_rank_summary`
    - `pair_escape_source_projected_direction`
    - `pair_escape_source_projected_step`
    - `pair_escape_source_projected_kept_values`
    - `pair_escape_source_projected_dropped_values`
- 修复了两个真实影响 projected 逻辑的问题：
  - `origin_priority=0` 之前会被 `or` 吃掉，导致 `lineage_projected` 排序优先级失效；
  - projected 值与 preserve 邻域重合时，旧去重逻辑会把它们从 exact1 escape 池抹掉；现在 `*_projected` 来源不会再被这一步误删。
- 进一步把 `_bounded_exact1_value_map()` 改成按来源优先级排序后再截断，确保 projected 值能先进入 single-byte 预筛选，而不是被 `escape_neighbor` 提前挤掉。

2) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "projected or soft_guard or local_escape or pair_entries or lineage_value"` -> `12 passed`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `44 passed`
- `python -m pytest -q` -> `116 passed`

3) 真实样本回归
- 新 run:
  - `solve_reports\\harness_runs\\samplereverse_exact1_projected_quality_20260424`
- 目标：
  - `E:\\xwechat_files\\wxid_9ky6h8wz58b912_a1e5\\msg\\file\\2026-04\\samplereverse.exe`
- 注意：
  - harness 末尾仍因已知 `gbk` 编码错误退出；
  - compare-aware artifacts 已完整生成，本轮结论有效。

4) 关键结果
- projected source 的方向压缩与近邻预筛选已经真实执行：
  - 所有 pair 上都能看到分方向、分步长的 projected 值；
  - `pair_escape_source_projected_direction` / `pair_escape_source_projected_step` 已落盘。
- 但 exact1 质量仍未突破：
  - `projected_local_compatible_count = 0`
  - `projected_distance_explosive_count = 24`
  - `pair_near_local_escape_count = 0`
  - `pair_wide_local_escape_count = 3`
  - `improved_pair_frontier_pool_count = 0`
- projected 值现在不再只是“生成后消失”，而是被明确诊断为质量不达标：
  - 所有 projected 值都落入 `projected_generated_but_distance_explosive`
  - `projected_local_compatible_but_ranked_out = []`
  - `projected_local_compatible_reached_pair_pool = []`
  - `pair_escape_source_projected_kept_values` 全为空
- 当前 best wide-local 仍是：
  - `[0,3] -> 5a3e7f43ddd474d0...`，`ci_distance5=708`, `raw_distance10=764`
  - `[0,2] -> 5a3e7c46ddd474d0...`，`ci_distance5=751`, `raw_distance10=751`
  - `[0,1] -> 5a3b7f46ddd474d0...`，`ci_distance5=870`, `raw_distance10=870`
- pair-set 轮换没有成为主瓶颈：
  - `primary_pair_set` 与 `alternate_pair_set` 的选择键完全一致；
  - 两边都是 `projected_local_compatible_count = 0`；
  - winner 仍只是默认 `primary_pair_set`。
- runtime best 仍未改善：
  - exact2:
    - `78d540b49c590770...` -> `runtime_ci_exact_wchars=2`, `runtime_ci_distance5=246`
  - exact1:
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`

5) 结论
- 本轮成功证明：下一步已经不该继续打 projected source 的“有无”与“方向/步长扇出”。
- projected 值现在已经：
  - 生成了；
  - 带着正确的 direction/step/origin 诊断进入 artifact；
  - 并在进入 single-byte 预筛选前得到真实质量打分。
- 当前更精确的瓶颈是：
  - projected 值的单字节质量仍全部过差；
  - 连一个 `projected_local_compatible` 都没有产生；
  - 因此 exact1 根本还没进入 “projected source -> near_local_escape -> refine” 的接力链。
- 下一轮默认方向：
  - 不再继续调 projected source 的投影框架；
  - 主攻 exact1 projected 值到 `local_compatible_soft` 的单字节质量压缩；
  - 优先考虑让 projected 值不再与纯 `escape_neighbor` 同权，而是根据 exact1 baseline 的 `ci_distance5/raw_distance10/structure_delta` 做更强的 local-compatible 过滤或降权；
  - 若下一轮仍然 `projected_local_compatible_count = 0`，再考虑改成“projected-only escape lane”或切回 pair 组合规则，而不是继续扩 source 家族。

## 2026-04-24 exact1 projected-vs-neighbor competitive handoff

1) 代码改动
- 在 `reverse_agent/strategies/compare_aware_search.py` 中补齐了 exact1 projected-vs-neighbor 计划里还没完全落地的两段 handoff：
  - 新增 `_exact1_projected_competition_summary()` / `_exact1_projected_competition_reason_from_runs()`，把 exact1 当前停滞明确区分成：
    - `single_byte_projected_competition`
    - `pair_gate_after_projected_winner`
    - `projected_winner_reached_pair_gate`
    - `pair_refine_after_projected_winner`
  - `pair_set_comparison_summary` 现在会被进一步归因为：
    - `pair_set_not_limiting_single_byte_competition`
    - `pair_set_rotation_secondary_to_projected_winner`
- 收紧了 exact1 的 SMT value pool handoff：
  - 新增 `_exact1_projected_winner_smt_entries()` / `_exact1_smt_preferred_entries()`；
  - SMT 只再吃：
    - `pair_profile_kept_preserve`
    - `pair_gate_kept_escape`
    - `pair_near_local_escape_candidates`
    - `projected_beats_neighbor` 的 projected winner
  - 不再把 `pair_best_local_escape` 这种 wide-local 诊断值直接送入 SMT；
  - projected runner-up / neighbor winner 也不会再通过这条 exact1 偏好通道泄漏进 SMT。

2) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "projected_competition or run_compare_aware_smt_records_feedback_value_pools_from_improved_frontier_entries"` -> `3 passed`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "projected_family_competition or soft_guard_prefers_projected_origin_over_escape_neighbor"` -> `3 passed`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `47 passed`
- `python -m pytest -q` -> `119 passed`

3) 真实样本回归
- 新 run:
  - `solve_reports\\harness_runs\\samplereverse_exact1_projected_vs_neighbor_20260424_rerun`
- 数据集：
  - `samplereverse_exact1_projected_vs_neighbor_20260424.json`
- 目标：
  - `E:\\xwechat_files\\wxid_9ky6h8wz58b912_a1e5\\msg\\file\\2026-04\\samplereverse.exe`
- 关键 artifact：
  - `solve_reports\\harness_runs\\samplereverse_exact1_projected_vs_neighbor_20260424_rerun\\reports\\tool_artifacts\\samplereverse\\frontier_guided_1_5a3e7f46ddd474d0\\samplereverse_compare_aware_guided_pool_result.json`
  - `solve_reports\\harness_runs\\samplereverse_exact1_projected_vs_neighbor_20260424_rerun\\reports\\tool_artifacts\\samplereverse\\samplereverse_compare_aware_strata_summary.json`
- 注意：
  - harness 末尾仍因已知 `gbk` 编码错误退出；
  - compare-aware / frontier / SMT artifacts 已完整生成，本轮结论有效。

4) 关键结果
- 这次已经不再是 “projected 全部生成后直接消失”：
  - `projected_beats_neighbor_count = 2`
  - `pair_projected_competitive_status` 首次明确出现两个 `projected_beats_neighbor`
  - 对应 projected winner 都已进入 `pair_guard_soft_promoted_values`
- 但 projected winner 仍未把 exact1 推进到 near-local：
  - `exact1_projected_local_compatible_count = 0`
  - `exact1_near_local_escape_count = 0`
  - `exact1_wide_local_escape_count = 3`
  - `pair_gate_kept_escape_count = 1`
- 新的竞争摘要已经成功把瓶颈改写清楚：
  - `frontier_exact1_stall_reason = projected_winner_reached_pair_gate`
  - `exact1_projected_competition_summary.stall_reason = projected_winner_reached_pair_gate`
  - `exact1_projected_competition_summary.pair_set_diagnosis = pair_set_rotation_secondary_to_projected_winner`
- projected 当前主要还是输在 distance，而不是 raw/structure：
  - `pair_projected_competitive_status` 中其余失败位置均落在 `projected_loses_on_distance`
- runtime best 仍未改善：
  - exact2:
    - `78d540b49c590770...` -> `runtime_ci_exact_wchars=2`, `runtime_ci_distance5=246`
  - exact1:
    - `5a3e7f46ddd474d0...` -> `runtime_ci_exact_wchars=1`, `runtime_ci_distance5=258`

5) 结论
- 本轮已经完成了原计划里最关键的“projected vs neighbor 正面竞争 + bounded handoff”闭环，并且真实样本确认：
  - projected winner 可以打赢同位置 neighbor；
  - artifact 能直接回答 projected 是赢了还是输在 distance/raw/structure；
  - SMT 不再把 wide-local / runner-up 诊断值当成 exact1 偏好输入。
- 当前默认下一步应转向：
  - 继续压 exact1 projected winner 进入 pair 后的质量，让 `projected_winner_reached_pair_gate` 进一步推进到 `near_local_escape`；
  - 优先研究为什么当前 projected winner 仍只能停在 wide-local / kept-local 边缘，而不是继续回头扩 projected source 家族；
  - pair-set 轮换本轮已不再是首要瓶颈，除非后续 artifact 重新显示 projected winner 数量掉回 0。

## 2026-04-24 exact1 projected pair-gate boundary diagnostics

1) 代码改动
- 在 `reverse_agent/strategies/compare_aware_search.py` 中实现 exact1 projected winner 的 pair-gate 去向诊断：
  - pair candidate 现在记录：
    - `pair_projected_winner_available`
    - `pair_projected_winner_contributions`
    - `pair_projected_boundary_mix`
    - `pair_projected_winner_gate_status`
  - 新增 gate status：
    - `projected_winner_kept_as_base_only`
    - `projected_winner_mixed_with_neighbor_wide`
    - `projected_winner_lost_after_pair_metrics`
    - `projected_winner_promoted_to_near_local`
- 新增 bounded `exact1_projected_boundary` 通道：
  - 只使用已有 `projected_beats_neighbor` winner；
  - 只组合：
    - projected winner + base
    - projected winner + current best neighbor
    - projected winner + projected runner-up
  - 不扩大 `EXACT1_PROJECTED_STEP_LIMIT`；
  - 不新增 samplereverse 专用硬编码。
- SMT handoff 继续保持通用：
  - 支持 nested `pair_projected_competitive_*` artifact；
  - 接收 `pair_projected_boundary_entries`；
  - 从 candidate-level `pair_projected_winner_available` 兜底提取 projected winner；
  - 继续排除 wide-local diagnostic。
- 在 SMT 调用前把 same-anchor guided context 合并回 validation-selected SMT base，避免 validation entry 丢失 pair-gate/projected diagnostics。

2) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "projected or near_local or smt_records"` -> `9 passed`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `49 passed`
- `python -m pytest -q` -> `121 passed`

3) 真实样本回归
- 最终有效 run：
  - `solve_reports\\harness_runs\\samplereverse_exact1_projected_pair_gate_20260424_smt`
- 数据集：
  - `samplereverse_exact1_projected_vs_neighbor_20260424.json`
- 目标：
  - `E:\\xwechat_files\\wxid_9ky6h8wz58b912_a1e5\\msg\\file\\2026-04\\samplereverse.exe`
- 注意：
  - harness 末尾仍因已知 `gbk` 编码错误退出；
  - compare-aware / frontier / SMT artifacts 已完整生成，本轮结论有效。

4) 关键结果
- runtime best 未改善：
  - exact2 仍为 `78d540b49c590770...`，`runtime_ci_exact_wchars=2`，`runtime_ci_distance5=246`
  - exact1 仍为 `5a3e7f46ddd474d0...`，`runtime_ci_exact_wchars=1`，`runtime_ci_distance5=258`
- frontier 仍停在：
  - `frontier_exact1_stall_reason = projected_winner_reached_pair_gate`
  - `exact1_projected_competition_summary.projected_beats_neighbor_count = 2`
  - `exact1_near_local_escape_count = 0`
  - `exact1_wide_local_escape_count = 3`
- 新诊断明确回答了 projected winner 的去向：
  - `[1,2]` kept escape 是 `projected_winner_kept_as_base_only`，即 pair gate 保留的是 base 值 `[62,127]`，没有真正消费 projected value `63`。
  - bounded boundary 生成了 `5a3f7c46ddd474d0`，即 `[1,2] -> [63,124]`，但被判为 `projected_winner_mixed_with_neighbor_wide`。
  - `pair_projected_boundary_entries = []`，说明没有 boundary candidate 达到 keep/near-local。
- SMT handoff 已确认吃到 projected winner：
  - `feedback_value_pools["1"]` 包含 `63`
  - SMT 仍返回 `targeted z3 finished with unknown`

5) 结论
- 本轮没有提升 runtime best，但完成了计划中的关键 observability 和 bounded boundary lane。
- 当前更精确瓶颈：
  - projected winner `63@pos1` 不再是“没被生成/没被传递”；
  - 它进入 boundary 和 SMT；
  - 但在 pair gate 里与 neighbor `124@pos2` 混合后仍表现为 wide-local，且 z3 在包含 `63` 的 value pool 下仍 unknown。
- 下一轮默认方向：
  - 不继续扩 projected source；
  - 转向 projected-only preserve lane / pair composition policy；
  - 重点比较 `projected winner + base` 为什么比 base-only差，以及是否需要让 projected winner 先以 preserve-style 单点 lane 稳住，再进入 pair escape。

## 2026-04-24 exact1 projected preserve lane + harness encoding fix

1) 代码改动
- 修复 harness 控制台输出编码问题：
  - `reverse_agent/harness.py` 新增 `_safe_console_log()`；
  - harness CLI 不再直接把日志交给裸 `print`；
  - 当 Windows 控制台是 `gbk` 且日志含不可编码字符时，会用 replacement 输出，不再让样本在报告末尾变成 error。
- 在 `reverse_agent/strategies/compare_aware_search.py` 中新增 projected-only preserve-style lane：
  - `projected_winner + base` 现在标记为 `exact1_projected_preserve_lane`；
  - mixed 组合仍保留为 `exact1_projected_boundary`；
  - 每个 pair 单独保留一个 projected preserve lane 名额，避免它被 `projected_winner + neighbor` 的 distance 排名挤掉；
  - 新增 artifact 字段：
    - `pair_projected_preserve_candidates`
    - `pair_projected_preserve_entries`
  - SMT preferred entries 现在也接收 `pair_projected_preserve_entries`。

2) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "projected or near_local or smt_records"` -> `9 passed`
- `python -m pytest -q tests/test_harness.py` -> `5 passed`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `49 passed`
- `python -m pytest -q` -> `122 passed`

3) 真实样本回归
- 新 run:
  - `solve_reports\\harness_runs\\samplereverse_exact1_projected_preserve_lane_20260424`
- 数据集：
  - `samplereverse_exact1_projected_vs_neighbor_20260424.json`
- 目标：
  - `E:\\xwechat_files\\wxid_9ky6h8wz58b912_a1e5\\msg\\file\\2026-04\\samplereverse.exe`
- harness 结果：
  - `error_cases = 0`
  - `completed_without_expected = 1`
  - 已确认之前的 `gbk` 编码错误不再终止 run。

4) 关键结果
- runtime best 仍未超过 baseline：
  - exact2 仍为 `78d540b49c590770...`，`runtime_ci_exact_wchars=2`，`runtime_ci_distance5=246`
  - exact1 仍为 `5a3e7f46ddd474d0...`，`runtime_ci_exact_wchars=1`，`runtime_ci_distance5=258`
- projected-only preserve lane 成功进入 near-local：
  - `exact1_near_local_escape_count = 2`
  - `pair_projected_preserve_entries` 包含：
    - `5a3f7f46ddd474d0...`
    - pair `[1,2] -> [63,127]`
    - `pair_candidate_origin = exact1_projected_preserve_lane`
    - `pair_projected_winner_gate_status = projected_winner_promoted_to_near_local`
  - `pair_projected_winner_gate_status_counts` 同时显示：
    - `projected_winner_kept_as_base_only`
    - `projected_winner_mixed_with_neighbor_wide`
    - `projected_winner_promoted_to_near_local`
- mixed boundary 仍差：
  - `[1,2] -> [63,124]` 仍为 `projected_winner_mixed_with_neighbor_wide`
  - `pair_projected_boundary_entries = []`
- SMT 仍未突破：
  - `feedback_value_pools["1"]` 包含 `63`
  - `summary = targeted z3 finished with unknown`

5) 结论
- 本轮完成了两个目标：
  - harness reporting/logging 的 `gbk` 失败已修复；
  - projected winner 已能通过 preserve-style 单点 lane 进入 near-local。
- 但这仍只是 exact1 局部推进，不是最终求解突破：
  - projected-only `[63, base]` 能进 near-local；
  - mixed `[63, neighbor]` 仍 wide；
  - SMT 吃到 `63` 后仍 unknown；
  - runtime best 未改善。
- 下一轮默认方向：
  - 在 projected preserve lane 基础上做 second-hop composition；
  - 优先尝试 “preserve-stabilized projected winner -> 再选第二字节”，而不是直接把 projected winner 和当前 neighbor 混合；
  - 同时补一个输出选择防护：模型只返回 `flag{` 这种前缀时，不应覆盖 runtime-evidence candidate。

## 2026-04-30 validation slot patch verification

1) 执行范围
- 按 `project_state/decision_packet.md` 跑 validation-slot patch 的最小验证。
- 没有改 `reverse_agent/strategies/compare_aware_search.py` 或测试代码；本轮只验证真实 harness artifact 并重建 `project_state`。

2) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `51 passed`
- `python -m pytest -q` -> `133 passed`

3) 真实样本回归
- 新 run:
  - `solve_reports\\harness_runs\\samplereverse_validation_slot_verify_20260430`
- 数据集：
  - `samplereverse_exact1_projected_vs_neighbor_20260424.json`
- 目标：
  - `E:\\xwechat_files\\wxid_9ky6h8wz58b912_a1e5\\msg\\file\\2026-04\\samplereverse.exe`
- 注意：
  - compare-aware / guided validation / frontier / SMT artifacts 已完整生成；
  - harness 最后模型调用因 Copilot CLI quota `402 You have no quota` 记为 `error_cases = 1`，但不影响本轮 validation-slot 证据。

4) 关键结果
- validation-slot patch 已真实生效：
  - `5a3f7f46ddd474d0` 已进入 `validation_candidates`；
  - 同一候选已出现在 `frontier_guided_1_5a3e7f46ddd474d0\\guided_pool_validation\\samplereverse_compare_aware_guided_pool_validation.json`。
- 该候选 validation 后没有收益：
  - `runtime_ci_exact_wchars = 0`
  - `runtime_ci_distance5 = 740`
  - `compare_semantics_agree = true`
- 当前 best 未改善：
  - exact2 仍为 `78d540b49c590770...`，`runtime_ci_exact_wchars=2`，`runtime_ci_distance5=246`
  - exact1 仍为 `5a3e7f46ddd474d0...`，`runtime_ci_exact_wchars=1`，`runtime_ci_distance5=258`

5) 结论
- 失败分类从 `B. selected_but_not_composed` 推进为 `D. validated_but_no_runtime_gain`。
- 不应继续修 validation ordering，也不应扩大 beam/budget。
- 下一轮默认方向：从已验证但无收益的 preserve-stabilized projected winner 出发，审计 second-hop composition，而不是继续验证槽位或回到 projected source 扩张。

## 2026-04-30 second-hop composition metadata-gated anchor patch

1) 审计结论
- `5a3f7f46ddd474d0` 已经 validated but no runtime gain：
  - `runtime_ci_exact_wchars = 0`
  - `runtime_ci_distance5 = 740`
  - `compare_semantics_agree = true`
- 真实断点在 frontier continuation：
  - 候选不是 runtime-improved frontier；
  - 也不是 `_frontier_anchor_candidates()` 选出的 best exact0；
  - 因而不会成为下一轮 frontier-guided anchor。

2) 代码改动
- 在 `reverse_agent/strategies/compare_aware_search.py` 中新增 metadata-gated second-hop anchor：
  - role: `validated_projected_preserve_second_hop`
  - 只接受 compare-agree 的 `projected_preserve_handoff`
  - 必须由 metadata/context 证明：
    - `exact1_projected_preserve_lane`
    - `projected_winner_with_base`
    - `projected_winner_promoted_to_near_local`
- 新增 `_frontier_continuation_candidates()`：
  - 只有在 `distance_not_improved` 且还有 frontier iteration 名额时，才允许 second-hop continuation；
  - 不改 beam/budget/topN/timeout；
  - 不替换 exact2 best，不把 handoff 当 final answer。

3) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "second_hop_composition or validated_projected_preserve_handoff"` -> `3 passed, 51 deselected`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `54 passed`
- `python -m pytest -q` -> `136 passed`

4) 结论
- second-hop composition gap 已做最小修复。
- 本轮未跑 harness；当前环境已知 Copilot quota 会在最终模型调用处 `402`，而该补丁已有本地单测覆盖。
- 下一轮有 quota 或需要 artifact 验证时，默认 run：
  - `samplereverse_second_hop_composition_audit_20260430`
  - 重点检查是否出现 `frontier_guided_2_5a3f7f46ddd474d0` 或等价 second-hop guided artifact。

## 2026-05-02 second-hop runtime verification

1) 执行范围
- 按 `project_state/decision_packet.md` 执行 second-hop runtime 验证。
- 本轮没有修改 strategy 或测试代码；只运行单测、真实 harness、重建 compact state，并更新中文执行报告。

2) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "second_hop_composition or validated_projected_preserve_handoff"` -> `3 passed, 51 deselected`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `54 passed`

3) 真实样本回归
- 新 run:
  - `solve_reports\\harness_runs\\samplereverse_second_hop_composition_verify_20260502`
- 数据集：
  - `samplereverse_exact1_projected_vs_neighbor_20260424.json`
- 结果：
  - `error_cases = 0`
  - `completed_without_expected = 1`
  - `candidate_quality = 1.0`
  - `evidence_coverage = 1.0`
  - 本轮没有出现 Copilot CLI quota `402`。

4) 关键结果
- 没有生成 `frontier_guided_2_5a3f7f46ddd474d0` 或等价二跳 guided artifact；新 run 目录下只有 `frontier_guided_1_5a3e7f46ddd474d0`。
- 但 `samplereverse_compare_aware_frontier_summary.json` 已记录：
  - `second_hop_frontier_candidates` 包含 `5a3f7f46ddd474d0`
  - `frontier_continuation_candidates` 包含 `5a3f7f46ddd474d0`
  - `used_second_hop_frontier_candidates = true`
  - role 为 `validated_projected_preserve_second_hop`
- `5a3f7f46ddd474d0` 在 validation 中保持：
  - `compare_semantics_agree = true`
  - `runtime_ci_exact_wchars = 0`
  - `runtime_ci_distance5 = 740`
- 当前 best 未改善：
  - exact2 仍为 `78d540b49c590770...`，`runtime_ci_exact_wchars=2`，`runtime_ci_distance5=246`
  - exact1 仍为 `5a3e7f46ddd474d0...`，`runtime_ci_exact_wchars=1`，`runtime_ci_distance5=258`

5) 结论
- second-hop candidate 生成和 metadata gate 已被真实 artifact 验证。
- 当前瓶颈不再是 projected preserve handoff 是否进入 continuation，而是 frontier loop stop condition / artifact emission：continuation candidate 已记录为 used，但没有实际落到第二轮 `frontier_guided_2`。
- 下一轮默认方向：
  - 审计 `_frontier_continuation_candidates()` 返回 `continue` 后，为什么循环仍以 `distance_not_improved` 收敛并退出；
  - 重点检查 loop stop condition 和 artifact emission；
  - 不扩大 beam/budget/topN/timeout，不回到 blind search，不重新争论 validation ordering。

## 2026-05-02 second-hop loop stop-condition fix + harness verification

1) 代码改动
- 在 `reverse_agent/strategies/compare_aware_search.py` 中修复 frontier loop stop condition：
  - `_frontier_continuation_candidates()` 返回 `used_second_hop=True` 且 reason 为 `continue` 时，不再被后续 “无 improved candidate” 归一化逻辑覆盖成 `distance_not_improved`。
  - 未扩大 `FRONTIER_MAX_ITERATIONS`、beam、budget、topN、timeout。
  - 未改 candidate generation、metadata gate、model path、GUI、pipeline 或 harness 总控。
- 在 `tests/test_compare_aware_search_strategy.py` 新增 strategy-level 回归：
  - 第一轮 frontier 无 runtime improvement；
  - 产生 `validated_projected_preserve_second_hop` continuation；
  - 断言第二轮 guided pool 以 `5a3f7f46ddd474d0` 为 anchor 运行；
  - 断言第一轮 `used_second_hop_frontier_candidates=true` 且 converge reason 保持 `continue`。

2) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "second_hop_composition or validated_projected_preserve_handoff or second_frontier_guided_round"` -> `5 passed, 50 deselected`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `55 passed`
- `python -m pytest -q` -> `137 passed`

3) 真实样本回归
- 新 run:
  - `solve_reports\\harness_runs\\samplereverse_second_hop_loop_fix_verify_20260502`
- 数据集：
  - `samplereverse_exact1_projected_vs_neighbor_20260424.json`
- 结果：
  - `error_cases = 0`
  - `completed_without_expected = 1`
  - `candidate_quality = 1.0`
  - `evidence_coverage = 1.0`
  - 本轮没有出现 Copilot CLI quota `402`。

4) 关键结果
- 新 artifacts 已生成二跳 guided 目录：
  - `frontier_guided_2_5a3f7f46ddd474d0`
- `samplereverse_compare_aware_frontier_summary.json` 显示：
  - `frontier_guided_runs` 数量为 2；
  - 第 1 轮 `used_second_hop_frontier_candidates = true`；
  - 第 1 轮 `frontier_converged_reason = continue`；
  - 第 2 轮 anchor 为 `5a3f7f46ddd474d0`；
  - 第 2 轮 role 为 `validated_projected_preserve_second_hop`；
  - 总体 `frontier_converged_reason = iteration_limit`。
- 当前 best 未改善：
  - exact2 仍为 `78d540b49c590770...`，`runtime_ci_exact_wchars=2`，`runtime_ci_distance5=246`
  - exact1 仍为 `5a3e7f46ddd474d0...`，`runtime_ci_exact_wchars=1`，`runtime_ci_distance5=258`
  - `5a3f7f46ddd474d0` 已实际作为 second-hop anchor 使用，但没有产生 runtime gain。

5) 结论
- loop stop condition / artifact emission 问题已修复并通过真实 harness 验证。
- 当前瓶颈推进为 second-hop candidate quality / projected winner 后 pair gate：
  - plumbing 已通；
  - 二跳已跑；
  - 但二跳未产生优于 existing exact1/exact2 的候选。
- 下一轮默认方向：
  - 审计 `frontier_guided_2_5a3f7f46ddd474d0` 的 pair/pool 输出；
  - 重点看 `pair_gate_after_projected_winner` 和二跳 candidate quality；
  - 不扩大 beam/budget/topN/timeout，不回到 blind search。

## 2026-05-02 second-hop pair/pool quality audit

1) 执行范围
- 按计划审计 `frontier_guided_2_5a3f7f46ddd474d0` 的二跳 pair/pool 输出。
- 本轮没有修改 strategy 或测试代码；没有重复运行 harness。
- 更新了 `project_state/codex_execution_report.md` 和 compact state。

2) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "second_hop_composition or validated_projected_preserve_handoff or second_frontier_guided_round"` -> `5 passed, 50 deselected`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `55 passed`

3) 关键审计结果
- 二跳 guided pool:
  - `top_entries = 16`
  - `validation_candidates = 8`
  - `pair_frontier_pool = 8`
  - `triad_frontier_pool = 8`
- 二跳 validation:
  - `compare_semantics_agree=true` 为 8 个；
  - `compare_semantics_agree=false` 为 0 个；
  - 只有 `5a3e7f46ddd474d0` 保持 exact1，`runtime_ci_distance5=258`；
  - 其余 7 个都是 exact0，最佳 distance5 为 419。
- 未发现任何未验证或被过滤的候选优于 existing exact1 `5a3e7f46ddd474d0`。
- `pair_gate_kept_escape = 0`、`pair_gate_failed_escape = 0`、`pair_drop_reasons = {}`：
  - 这不是 gate 错误丢弃候选；
  - 二跳 escape source 状态是 `profile_source_empty`，没有 local/hard escape candidate 到达 gate。
- `frontier_refine_2` 继续保留：
  - exact2 best `78d540b49c590770...`，`runtime_ci_exact_wchars=2`，`runtime_ci_distance5=246`
  - exact1 best `5a3e7f46ddd474d0...`，`runtime_ci_exact_wchars=1`，`runtime_ci_distance5=258`

4) 结论
- 当前瓶颈从疑似 `pair_gate_after_projected_winner` 收敛为：
  - `candidate_quality_insufficient_after_projected_winner`
- 不应修改 pair gate/refine selection。
- 不应扩大 beam/budget/topN/timeout/iteration limit。
- 下一轮默认方向：
  - 单独决策 second-hop candidate source / projection hypothesis；
  - 或审计 transform/profile 假设，解释为什么 projected preserve 二跳退化为 exact1 邻域噪声。

## 2026-05-02 second-hop candidate source / projection hypothesis audit

1) 执行范围
- 按计划审计 `frontier_guided_2_5a3f7f46ddd474d0` 的 candidate source / projection hypothesis。
- 本轮没有修改 strategy 或测试代码；没有重复运行 harness。
- 更新了 `project_state/codex_execution_report.md`、`project_state/current_state.json`、`project_state/task_packet.json`、`project_state/model_gate.json` 和本日志。

2) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "second_hop_composition or validated_projected_preserve_handoff or second_frontier_guided_round"` -> `5 passed, 50 deselected`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `55 passed`

3) 关键审计结果
- 二跳 source quality:
  - `top_entries = 16`，validated 8，compare-agree 8，best 仍是已知 exact1 `5a3e7f46ddd474d0`。
  - `pair_frontier_pool = 8`，唯一 validated agree 仍是 `5a3e7f46ddd474d0`。
  - `triad_frontier_pool = 8`，6 个 validated agree，但全部 exact0；最佳新候选是 `5a3f7fc2ddd474d0`，`runtime_ci_distance5=419`。
  - `pair_escape_source_statuses = profile_source_empty` 覆盖 `0,1`、`0,2`、`0,3`。
- `profile_source_empty` 的含义已澄清：
  - 不是 pair gate 漏收；
  - 当前 bad anchor 的 preserve 邻域已经回落到 known exact1；
  - 没有有效 local/hard escape profile entries 到达 gate。
- projected source 证据：
  - projected values 确实生成，并有 6 个 reached pair pool；
  - 但 validated 后仍未超过 exact1/exact2；
  - `projected_generated_but_distance_explosive` 和 ranked out 不是误分类 evidence。

4) 结论
- 没有发现合法 compare-agree source 被误分类、过早 rank out 或未进入二跳 pool。
- 无代码修复建议。
- `5a3f7f46ddd474d0` 降级为：validated but no runtime gain / local unproductive projected preserve direction。
- 当前 best 未改善：
  - exact2 仍为 `78d540b49c590770...`，`runtime_ci_exact_wchars=2`，`runtime_ci_distance5=246`
  - exact1 仍为 `5a3e7f46ddd474d0...`，`runtime_ci_exact_wchars=1`，`runtime_ci_distance5=258`

5) 下一轮默认方向
- 单一推荐方向：审计 exact2 seed `78d540b49c590770` 的 source quality，不回 blind search。
- 重点比较 exact2 seed source lanes 与 projected preserve lane，解释为什么 exact2 仍保持 runtime exact2，而 projected preserve 二跳退化为 exact1/exact0。
- 不扩大 beam/budget/topN/timeout/iteration limit。
- 不运行新 harness，除非先形成 exact2-source hypothesis 或发现小范围 source fix。

## 2026-05-02 exact2 seed source quality audit

1) 执行范围
- 按计划审计 exact2 seed `78d540b49c590770` 的 source quality。
- 本轮没有修改 strategy、profile、transform 或测试代码；没有运行新 harness。
- 更新了 `project_state/codex_execution_report.md`、`project_state/current_state.json`、`project_state/task_packet.json`、`project_state/model_gate.json` 和本日志。

2) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "exact2 or frontier or pairscan"` -> `23 passed, 32 deselected`

3) 关键审计结果
- `78d540b49c590770` 是 `SamplereverseProfile` 传入的首位 profile seed anchor。
- bridge pairscan 围绕该 anchor 运行所有 byte pairs，记录：
  - `hot_positions = [0, 1]`
  - 唯一 pairscan top/best 为 `78d540b49c59077041414141414141`
  - runtime `compare_semantics_agree=true`
  - `runtime_ci_exact_wchars=2`
  - `runtime_ci_distance5=246`
- seed-guided validation 中，未改变的 `78d540b49c590770` 保持 exact2 / distance5 246；周边 guided mutations 立即掉到 exact0。
- frontier1 从 exact2 seed 产生 `5a3e7f46ddd474d0`，但只到 exact1 / distance5 258。
- frontier2/projected preserve 二跳只恢复 known exact1 或产生 exact0；最佳新候选仍是 `5a3f7fc2ddd474d0`，exact0 / distance5 419。

4) 代码路径核对
- `run_compare_aware_bridge()` 没有丢弃更好的 pair/triad/quartet/quint candidate；artifact 中没有更优 source。
- `_bridge_progress()` 正确没有把 exact2 / distance5 246 当作 early solved，因为只有 exact3+ 或 distance 小于 baseline 才会提前结束。
- `_diverse_validation_candidates()` / `_frontier_anchor_candidates()` 保留 exact2/exact1/exact0 representatives；没有发现 lane 误分类。
- `frontier_guided_2` 的 `profile_source_empty` 仍解释为 source quality 不足，不是 pair gate 漏收。

5) 结论
- 没有发现 exact2 seed source 的可修复问题。
- 无代码修复建议。
- 当前 best 未改善：
  - exact2 仍为 `78d540b49c590770...`，`runtime_ci_exact_wchars=2`，`runtime_ci_distance5=246`
  - exact1 仍为 `5a3e7f46ddd474d0...`，`runtime_ci_exact_wchars=1`，`runtime_ci_distance5=258`
  - `5a3f7f46ddd474d0` 继续保持 downgraded，不作为 final/best。

6) 下一轮默认方向
- 审计 transform/profile boundary for exact2 basin：
  - 以 `78d540b49c590770` 作为稳定 exact2 reference；
  - 以 `5a3e7f46ddd474d0` 和 `5a3f7f46ddd474d0` 作为 exact1/projected preserve contrast cases；
  - 解释为什么当前 compare-aware score 只能维持 `flag{` prefix shape，而无法推进到 exact3+。
- 不扩大 beam/budget/topN/timeout/iteration limit。
- 不回 blind search。
- 不运行新 harness，除非先形成具体 transform/profile hypothesis 或小范围 source fix。

## 2026-05-02 exact2 basin transform/profile boundary diagnostics

1) 执行范围
- 按计划实现 exact2 basin transform/profile boundary 的 diagnostic-only 改动。
- 没有扩大 beam/budget/topN/timeout/iteration limit。
- 没有修改 final candidate selection 规则。
- 没有运行新 harness。
- 更新了 `project_state/codex_execution_report.md`、`project_state/current_state.json`、`project_state/task_packet.json`、`project_state/model_gate.json` 和本日志。

2) 代码改动
- 在 `reverse_agent/strategies/compare_aware_search.py` 中新增 prefix boundary diagnostics：
  - 对 raw prefix 拆解 UTF-16 wchar pair；
  - 记录 raw/target pair、case-insensitive low-byte distance、high-byte distance、pair distance、exact_ci、contiguous_exact；
  - 同时保留 `ci_exact_wchars`、`ci_distance5`、`raw_distance10` 和 wide-structure metrics。
- `validate_compare_aware_results()` 每条 validation 现在附带 `prefix_boundary`。
- frontier/refine summary 和 strategy metadata 现在附带 `prefix_boundary_diagnostics`。
- SMT payload 现在附带 primary base 的 `prefix_boundary`。
- 当 primary SMT base 不是 exact2，但存在 compare-agree exact2 时，新增 diagnostic-only `exact2_basin_smt` payload：
  - 记录 exact2 base anchor；
  - 记录 primary SMT base；
  - 记录 exact2 basin variable byte/nibble positions 和 feedback value pools；
  - 不运行额外 validation，不替换 best。

3) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "smt or exact2 or boundary or frontier"` -> `26 passed, 31 deselected`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `57 passed`
- `python -m pytest -q` -> `139 passed`

4) 关键诊断基线
- 现有 artifact 对照仍为：
  - `78d540b49c590770`: runtime prefix `4600 6c00 4464 830d 311c`，exact2 / distance5 246。
  - `5a3e7f46ddd474d0`: runtime prefix `4600 6135 7f0b 8c68 8502`，exact1 / distance5 258。
  - `5a3f7f46ddd474d0`: runtime prefix `7493 4b15 6ba6 9ef3 370f`，exact0 / distance5 740。
  - `5a3f7fc2ddd474d0`: runtime prefix `854a e01a bc18 692c 7505`，exact0 / distance5 419。
- 本轮没有生成新 runtime artifact；上述是旧 artifact 的审计基线。

5) 结论
- 当前 best 未改善：
  - exact2 仍为 `78d540b49c590770...`，runtime exact2 / distance5 246；
  - exact1 仍为 `5a3e7f46ddd474d0...`，runtime exact1 / distance5 258；
  - `5a3f7f46ddd474d0` 继续 downgraded。
- 本轮只增加诊断能力，尚不能证明是 scorer bug；下一轮需要看新 diagnostics artifact。

6) 下一轮默认方向
- 运行或审计下一份包含 `prefix_boundary_diagnostics` 和 `exact2_basin_smt` 的 compare-aware artifact。
- 如果 exact2-basin diagnostic positions/value pools 看起来有推进信号，再实现真实 bounded exact2 SMT pass；仍不扩大 timeout。
- 如果 diagnostics 没有新信号，则分类为 `candidate_quality_insufficient_after_transform_boundary`，不继续改代码，等待新的 profile/transform hypothesis。

## 2026-05-02 prefix boundary diagnostics artifact + rg tool fix

1) 执行范围
- 按计划修复本地 `rg` 工具链，并运行新的 diagnostics-only compare-aware harness。
- 没有扩大 beam/budget/topN/timeout/frontier iteration limit。
- 没有修改 candidate generation、ranking、runtime validation 或 final selection 规则。

2) `rg` 修复
- 问题：`rg` 只解析到 Codex App 的 WindowsApps resource path，Windows 拒绝直接执行。
- 修复：复制已验证可运行的 Copilot ripgrep x64 到用户级 PATH：
  - `C:\Users\wjc27\AppData\Local\OpenAI\Codex\bin\rg.exe`
- 验证：
  - `rg --version` -> `ripgrep 15.0.0`
  - `rg --files` 在 `F:\reverse-agent` 下可运行。

3) 代码改动
- 在 `reverse_agent/strategies/compare_aware_search.py` 中修复 metadata emission：
  - 之前 `exact2_basin_smt` 只加入了 in-memory `smt_run["payload"]`；
  - 但 `samplereverse_compare_aware_smt_result.json` 已在此之前写出，导致真实 artifact 缺少该字段；
  - 现在在附加 `exact2_basin_smt` 后回写 SMT result JSON。
- 在 `tests/test_compare_aware_search_strategy.py` 中补充断言：
  - strategy metadata 含 `exact2_basin_smt`；
  - 磁盘 `smt_result.json` 也含 `exact2_basin_smt`。

4) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "smt or exact2 or boundary or frontier"` -> `26 passed, 31 deselected`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `57 passed`
- `python -m pytest -q` -> `139 passed`

5) Harness
- 第一轮 run：`samplereverse_prefix_boundary_diagnostics_20260502`
  - 结果：`prefix_boundary_diagnostics` 存在，但 `exact2_basin_smt` 未落盘；
  - 结论：metadata emission gap，先修 reporting，不继续搜索。
- 修复后 run：`samplereverse_prefix_boundary_diagnostics_emitfix_20260502`
  - summary: `solve_reports\harness_runs\samplereverse_prefix_boundary_diagnostics_emitfix_20260502\summary.json`
  - SMT artifact: `solve_reports\harness_runs\samplereverse_prefix_boundary_diagnostics_emitfix_20260502\reports\tool_artifacts\samplereverse\smt\samplereverse_compare_aware_smt_result.json`
  - `prefix_boundary_diagnostics`: present, 16 entries
  - `exact2_basin_smt`: present
  - harness completed 1 case, 0 errors

6) exact2 basin diagnostic result
- `exact2_basin_smt`:
  - `attempted=false`
  - `recommended=true`
  - `base_anchor=78d540b49c590770`
  - `primary_base_anchor=5a3e7f46ddd474d0`
  - `runtime_ci_exact_wchars=2`
  - `runtime_ci_distance5=246`
  - `variable_byte_positions=[1,2,3,0,4]`
  - `variable_nibble_positions=[2,3,0,1,4]`
  - `feedback_value_pools={1:[213,62,60],2:[64,127,128],3:[180,143],0:[120],4:[156]}`

7) 结论
- Classification: `diagnostics_show_promising_exact2_basin_smt`
- 当前 runtime best 未改善：
  - exact2 仍为 `78d540b49c59077041414141414141`，runtime exact2 / distance5 246。
- 本轮 case `selected_flag` 为模型/候选评分选出的 `flag{`；这不是 compare-aware runtime best 改善，下一轮不要把它当作突破。

8) 下一轮默认方向
- 实现真实 bounded exact2 SMT pass：
  - 使用本轮 `exact2_basin_smt` 的 byte/nibble positions 和 value pools；
  - 不扩大 beam/budget/topN/timeout/frontier iteration limit；
  - 不改变 final selection 规则；
  - 目标是验证 exact2 basin 是否能推进到 exact3+。

## 2026-05-03 real bounded exact2 basin SMT pass

1) 执行范围
- 按计划实现真实 bounded exact2 basin SMT pass。
- 保留 primary frontier SMT，不改变 final selection 规则。
- 没有扩大 beam/budget/topN/timeout/frontier iteration limit。
- 更新了 `project_state/codex_execution_report.md`、`project_state/current_state.json`、`project_state/task_packet.json`、`project_state/model_gate.json`、`project_state/artifact_index.json` 和本日志。

2) 代码改动
- `reverse_agent/samplereverse_z3.py`
  - `solve_targeted_prefix8()` 新增 `value_pools` 参数。
  - 对 selected byte 添加 pool 约束，并始终把 base byte 并入 pool，避免排除基线。
  - Z3 evidence 现在记录实际使用的 bounded value pools。
- `reverse_agent/strategies/compare_aware_search.py`
  - `run_compare_aware_smt()` 支持 override byte/nibble positions 和 value pools。
  - 当 `exact2_basin_smt.recommended=true` 时，新增单独 `smt_exact2_basin` pass。
  - exact2-basin validations 会在有候选时进入 final candidate aggregation。
- `tests/test_compare_aware_search_strategy.py`
  - 补充 value pool 传递和 exact2-basin SMT pass 的覆盖。

3) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "smt or exact2 or boundary or frontier"` -> `26 passed, 32 deselected`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `58 passed`
- `python -m pytest -q` -> `140 passed`

4) Harness
- Run: `samplereverse_exact2_basin_smt_20260503`
- Summary: `solve_reports\harness_runs\samplereverse_exact2_basin_smt_20260503\summary.json`
- Primary SMT artifact:
  - `solve_reports\harness_runs\samplereverse_exact2_basin_smt_20260503\reports\tool_artifacts\samplereverse\smt\samplereverse_compare_aware_smt_result.json`
  - base `5a3e7f46ddd474d0`
  - summary `targeted z3 finished with unknown`
- exact2-basin SMT artifact:
  - `solve_reports\harness_runs\samplereverse_exact2_basin_smt_20260503\reports\tool_artifacts\samplereverse\smt_exact2_basin\samplereverse_compare_aware_smt_result.json`
  - base `78d540b49c590770`
  - byte positions `[1,2,3,0,4]`
  - nibble positions `[2,3,0,1,4]`
  - value pools `0:78, 1:d5/3e/3c, 2:40/7f/80, 3:b4/8f, 4:9c`
  - summary `targeted z3 finished with unknown`
  - validation candidates `[]`
- Harness completed 1 case, 0 errors.

5) 结论
- Classification: `candidate_quality_insufficient_after_exact2_basin_smt`
- 当前 runtime best 未改善：
  - exact2 仍为 `78d540b49c59077041414141414141`，runtime exact2 / distance5 246。
- `selected_flag` 仍为模型/候选评分选出的 `flag{`；仍不能视为 runtime improvement。
- 注意：Z3 `unknown` 不是 unsat proof，只能说明当前 bounded SMT formulation 没有产出候选。

6) 下一轮默认方向
- 审计 `smt_exact2_basin_unknown`：
  - 不通过扩大 timeout 解决；
  - 优先考虑对当前 tiny value pools 做 deterministic bounded execution/evaluation；
  - 或收缩 SMT formulation/objective，使 exact2-basin bounded pass 能产出可验证候选或明确失败证据。

## 2026-05-03 exact2 basin value-pool deterministic evaluation

1) 执行范围
- 按计划审计 `smt_exact2_basin_unknown`，并实现 deterministic exact2 basin value-pool evaluator。
- 没有扩大 beam/budget/topN/timeout/frontier iteration limit。
- 没有回到 blind search。
- 更新了 `project_state/codex_execution_report.md`、`project_state/current_state.json`、`project_state/task_packet.json`、`project_state/model_gate.json`、`project_state/artifact_index.json`、`project_state/negative_results.json` 和本日志。

2) 代码改动
- `reverse_agent/samplereverse_z3.py`
  - `Z3ProbeResult` 新增 `diagnostics`。
  - `solve_targeted_prefix8()` 记录 solver type、timeout、symbolic compare bytes、selected byte/nibble counts、value-pool sizes、estimated combinations 和 `z3_reason_unknown`。
  - `_decrypt_prefix()` 默认解 10 bytes，以匹配 5-wchar compare target。
- `reverse_agent/strategies/compare_aware_search.py`
  - SMT payload 透出 Z3 diagnostics。
  - 新增 `run_exact2_basin_value_pool_evaluation()`。
  - exact2-basin SMT attempted 且无 validation candidates、组合数 <= 128 时，枚举 diagnostic value pools 并走现有 runtime validation。
  - evaluator validations 只有 runtime compare-agree 且 exact_wchars > baseline 或 distance5 < baseline 时才进入 promotable final aggregation。
- `tests/test_compare_aware_search_strategy.py`
  - 覆盖 SMT unknown diagnostics、bounded pool enumeration、runtime validation handoff、无提升不 promotion。

3) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "smt or exact2 or boundary or frontier or value"` -> `37 passed, 23 deselected`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `60 passed`
- `python -m pytest -q` -> `142 passed`

4) Harness
- Run: `samplereverse_exact2_basin_valuepool_eval_20260503`
- Summary: `solve_reports\harness_runs\samplereverse_exact2_basin_valuepool_eval_20260503\summary.json`
- exact2-basin SMT artifact:
  - `solve_reports\harness_runs\samplereverse_exact2_basin_valuepool_eval_20260503\reports\tool_artifacts\samplereverse\smt_exact2_basin\samplereverse_compare_aware_smt_result.json`
  - summary `targeted z3 finished with unknown`
  - `z3_reason_unknown=unknown`
  - `estimated_value_pool_combinations=18`
- exact2-basin value-pool artifact:
  - `solve_reports\harness_runs\samplereverse_exact2_basin_valuepool_eval_20260503\reports\tool_artifacts\samplereverse\exact2_basin_value_pool\samplereverse_exact2_basin_value_pool_result.json`
  - generated `18`, unique `18`, runtime validated `18`
  - best runtime candidate `78d540b49c59077041414141414141`
  - best runtime exact2 / distance5 246
  - `improved_over_exact2=false`
- Harness completed 1 case, 0 errors.

5) 结论
- Classification: `exact2_basin_value_pools_exhausted_no_gain`
- 当前 runtime best 未改善：
  - exact2 仍为 `78d540b49c59077041414141414141`，runtime exact2 / distance5 246。
- `selected_flag` 仍为模型/候选评分选出的 `flag{`；仍不能视为 runtime improvement。
- 已将该 exact2 value-pool branch 记录到 `project_state/negative_results.json`。

6) 下一轮默认方向
- 停止重复当前 exact2 basin value-pool branch，除非 diagnostics value pools 改变。
- 下一步需要新的 bounded profile/transform hypothesis 或不同 evidence source。
- 不扩大 timeout/beam/budget/topN/frontier iteration limit。
- 不回 blind search。

## 2026-05-03 profile / transform hypothesis audit

1) 执行范围
- 按 `decision_packet.md` 执行最小、bounded、metadata-only profile/transform audit。
- 没有改变 candidate generation、ranking、final selection。
- 没有扩大 beam、budget、topN、timeout、frontier iteration limit。
- 没有重复已穷尽的 exact2 basin value-pool branch；仅在新 run 中沿现有策略再次确认并写入 stop evidence。

2) 代码改动
- `reverse_agent/transforms/samplereverse.py`
  - 新增 sample-specific `trace_candidate_transform()`。
  - trace 输出 candidate layout、nibble expansion、UTF-16LE raw bytes、Base64 prefix/length/padding/chunk boundary、RC4 key/decrypt prefix、5-wchar deltas 和 score metrics。
- `reverse_agent/strategies/compare_aware_search.py`
  - 新增 `profile_transform_hypothesis_matrix.json` artifact 生成。
  - 候选数硬上限 8。
  - 候选来源包含当前 exact2 best、exact1 frontier、secondary exact2、禁止 promotion 的 projected examples、probe baseline。
  - matrix 覆盖 H1-H6，并记录 evidence for/against、artifacts、bounded validation、success signal、stop condition。
- `tests/test_compare_aware_search_strategy.py`
  - 覆盖 transform trace、known runtime prefix 对齐、bounded matrix artifact、metadata-only 不改变搜索/selection。

3) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "profile or transform or boundary or trace or compare"` -> `62 passed`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `62 passed`
- `python -m pytest -q` -> `144 passed`

4) Harness
- Run: `samplereverse_profile_transform_hypothesis_audit_20260503`
- Summary: `solve_reports\harness_runs\samplereverse_profile_transform_hypothesis_audit_20260503\summary.json`
- Artifact:
  - `solve_reports\harness_runs\samplereverse_profile_transform_hypothesis_audit_20260503\reports\tool_artifacts\samplereverse\profile_transform_hypothesis_matrix.json`
- Matrix facts:
  - `candidate_count=8`, cap 8。
  - H1-H6 complete。
  - `audit_only=true`。
  - `candidate_generation_changed=false`、`ranking_changed=false`、`final_selection_changed=false`。
  - `beam_budget_topn_timeout_frontier_limit_expanded=false`。
- Harness completed 1 case, 0 errors.

5) 结论
- Classification: `profile_transform_hypothesis_audit_complete`
- exact2 value-pool branch stopped:
  - attempted true, generated 18, unique 18, runtime validated 18。
  - best remained `78d540b49c59077041414141414141`。
  - runtime exact2 / distance5 246。
  - improved_over_exact2 false。
  - `project_state/negative_results.json` 已恢复/保留 do_not_repeat 记录。
- Trace:
  - current exact2 offline prefix `46006c004464830d311c`。
  - runtime prefix `46006c004464830d311c`。
  - helper/runtime consistent；H4/H5 降级。
  - prefix8 raw length 32 bytes，Base64 chunk remainder 2，prefix8 不在 Base64 chunk boundary 结束。
- `selected_flag` 仍为模型/候选评分选出的 `flag{`；仍不能视为 runtime improvement。

6) 下一轮默认方向
- 只做 H1/H3 的最小 bounded validation：
  - `prefix8 + Base64 chunk boundary` tiny contrast candidates。
  - candidate count <= 8。
  - runtime validation required。
  - success signal: runtime exact_wchars > 2 或 distance5 < 246，且 compare_semantics_agree 不失配。
- 若任何 trace 显示 helper/runtime 不一致，停止搜索并优先修正 transform/RC4 trace 假设。

## 2026-05-04 H1/H3 bounded boundary validation

1) 执行范围
- 按 `decision_packet.md` 执行 `prefix8 + Base64 chunk boundary` 的最小 bounded runtime validation。
- 新增固定 8 个 hand-picked H1/H3 contrast candidates，并全部 runtime validate。
- 没有新增 blind search、guided pool expansion、SMT expansion。
- 没有扩大 beam、budget、topN、timeout、frontier iteration limit。

2) 代码改动
- `reverse_agent/strategies/compare_aware_search.py`
  - 新增 `H1_H3_BOUNDARY_VALIDATION_FILE_NAME`、固定 8-candidate contrast set。
  - 新增 `run_h1_h3_boundary_validation()`。
  - 对每个候选记录 `trace_prefix7`、`trace_prefix8`、`trace_prefix9`，覆盖 Base64 raw remainder 1/2/0。
  - validation record 透出 layout hypothesis、boundary focus 和 transform traces。
  - 仅 runtime compare-agree 且 exact_wchars > 2 或 distance5 < 246 的 candidate 可 promotion。
- `tests/test_compare_aware_search_strategy.py`
  - 覆盖固定候选数、baseline presence、prefix 7/8/9 trace、`validate_top=8`、无提升不 promotion、有 runtime 提升才 promotion。

3) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "h1 or h3 or boundary or trace or compare"` -> `64 passed`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `64 passed`
- `python -m pytest -q` -> `146 passed`

4) Harness
- Run: `samplereverse_h1_h3_boundary_validation_20260504`
- Summary: `solve_reports\harness_runs\samplereverse_h1_h3_boundary_validation_20260504\summary.json`
- Artifact:
  - `solve_reports\harness_runs\samplereverse_h1_h3_boundary_validation_20260504\reports\tool_artifacts\samplereverse\h1_h3_boundary_validation\h1_h3_boundary_validation.json`
  - `solve_reports\harness_runs\samplereverse_h1_h3_boundary_validation_20260504\reports\tool_artifacts\samplereverse\h1_h3_boundary_validation\validation\h1_h3_boundary_validation.json`
- Harness completed 1 case, 0 errors.

5) 结论
- Classification: `h1_h3_boundary_contrast_exhausted_no_gain`
- H1/H3 fixed contrast candidates: 8 generated, 8 runtime validated。
- Best runtime candidate: `78d540b49c59077040414141414141`。
- Best runtime metrics: exact2 / distance5 246。
- 当前 runtime best 未改善：exact2 仍为 `78d540b49c59077041414141414141` / distance5 246。
- `selected_flag` 仍为模型/候选评分选出的 `flag{`；仍不能视为 runtime improvement。
- 已将该 H1/H3 fixed contrast set 记录到 `project_state/negative_results.json`。

6) 下一轮默认方向
- 停止重复当前 fixed 8-candidate H1/H3 boundary contrast set，除非 transform trace 或 candidate set 发生实质变化。
- 停止重复当前 exact2 basin value-pool branch，除非 diagnostics value pools 改变。
- 下一步需要新的 single bounded hypothesis 或不同 evidence source。
- 不扩大 timeout/beam/budget/topN/frontier iteration limit。

## 2026-05-04 transform trace consistency diagnostic

1) 执行范围
- 按计划执行 transform/runtime consistency audit。
- 复用既有 `trace_candidate_transform()`，没有新增搜索模式。
- 没有改变 candidate generation、ranking、final selection、promotion。
- 没有扩大 beam、budget、topN、timeout、frontier iteration limit。
- 没有重复 H1/H3 fixed 8-candidate runtime validation；仅读取既有 runtime-backed validation rows 做一致性对照。

2) 代码改动
- `reverse_agent/transforms/samplereverse.py`
  - trace 增加 candidate raw bytes、prefix length table 1-10、Base64 mod/padding、RC4 assumptions、compare window metadata。
- `reverse_agent/strategies/compare_aware_search.py`
  - 新增 `TRANSFORM_TRACE_CONSISTENCY_FILE_NAME` 和固定 5-candidate audit set。
  - 新增 `run_transform_trace_consistency_diagnostic()`，输出 `transform_trace_consistency.json`。
  - 对每个候选记录 offline/runtime prefix、metrics agree、compare_semantics_agree、first_unsupported_stage、evidence_status。
  - diagnostic 不产生 promotable candidates。
  - 当 negative_results 记录 H1/H3 fixed contrast 已穷尽时，不重复该 runtime validation。
- `reverse_agent/project_state.py`
  - artifact index 识别 transform trace consistency、profile audit、exact2 value-pool、H1/H3 artifacts。
  - current_state 将 `transform_model_confirmed` 作为当前 bottleneck。
  - build_negative_results 保留 exact2 value-pool、H1/H3 fixed contrast、当前 transform audit 的 do-not-repeat 记录。
- `tests/test_compare_aware_search_strategy.py`
  - 覆盖 trace prefix table、diagnostic runtime 对照、missing evidence、不产生 promotion。

3) 自动化验证
- `python -m pytest -q tests/test_compare_aware_search_strategy.py -k "transform_trace or profile_transform or h1_h3 or exact2_basin_smt_diagnostic"` -> `6 passed, 60 deselected`
- `python -m pytest -q tests/test_compare_aware_search_strategy.py` -> `66 passed`
- `python -m pytest -q` -> `148 passed`

4) Harness
- Run: `samplereverse_transform_trace_consistency_20260504`
- Summary: `solve_reports\harness_runs\samplereverse_transform_trace_consistency_20260504\summary.json`
- Artifact:
  - `solve_reports\harness_runs\samplereverse_transform_trace_consistency_20260504\reports\tool_artifacts\samplereverse\transform_trace_consistency\transform_trace_consistency.json`
- Harness completed 1 case, 0 errors.

5) 结论
- Classification: `transform_model_confirmed`
- 5 个 diagnostic candidates 全部有 runtime-backed evidence。
- mismatches `[]`，missing_runtime_evidence `[]`。
- 当前 runtime best 未改善：
  - exact2 仍为 `78d540b49c59077041414141414141`，runtime exact2 / distance5 246。
- `selected_flag` 仍为模型/候选评分选出的 `flag{`；仍不能视为 runtime improvement。

6) 下一轮默认方向
- 当前 exact2 plateau 不应再优先归因于 offline transform mismatch。
- 停止重复：
  - exact2 basin value-pool branch；
  - H1/H3 fixed 8-candidate boundary contrast set；
  - 当前 5-candidate transform trace consistency audit，除非 runtime evidence 改变。
- 下一步需要不同 bounded evidence source；建议做 focused dynamic probe，捕获 compare path 附近的 pre-RC4/Base64/key material。
- 不扩大 timeout/beam/budget/topN/frontier iteration limit。

